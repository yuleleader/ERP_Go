# 站内信模板样式统计

## 文档说明

本文档统计电商管理系统中所有站内信通知的模板样式，包括事件类型、标题格式、内容模板、接收者范围等信息。

---

## 一、事件类型总览

| 事件类型标识 | 事件类型名称 | 触发场景 | 图标 |
|-------------|-------------|---------|------|
| `image_uploaded` | 图片上传完成 | 工厂端上传生产实拍图片 | 🖼️ Picture |
| `order_shipped` | 订单已发货 | 发货端更新订单状态为已发货/虚拟发货 | 🚐 Van |
| `produce_status_changed_manual` | 生产状态人工调整 | 手动修改生产状态 | 🔔 Bell |
| `produce_status_changed_factory_image` | 订单生产进度更新 | 工厂上传生产图片自动触发 | 🔔 Bell |
| `produce_status_changed_shipping` | 订单生产全部完成 | 发货后自动锁定生产状态 | 🔔 Bell |

---

## 二、通知模板详细说明

### 2.1 图片上传完成通知

**事件类型**：`image_uploaded`

**触发条件**：工厂端上传生产实拍图片

**接收者**：订单创建者（`order.created_by`）

**标题模板**：
```
图片上传完成
```

**内容模板**：
```
订单 {order_id} 的图片已上传完成。
时间: {当前时间}
```

**示例**：
```
订单 100120260715252445123@qq.com12345 的图片已上传完成。
时间: 2026-07-15 14:30:00
```

---

### 2.2 订单已发货通知

**事件类型**：`order_shipped`

**触发条件**：发货端将订单状态更新为"已发货"或"虚拟发货"

**接收者**：订单创建者（`order.created_by`）

**标题模板**：
```
订单已发货
```

**内容模板**：
```
订单 {order_id} 已发货。
{物流公司: xxx}
{物流单号: xxx}
时间: {当前时间}
```

**说明**：物流公司和物流单号为可选字段，如有则显示，无则不显示。

**示例**：
```
订单 100120260715252445123@qq.com12345 已发货。
物流公司: 顺丰速运
物流单号: SF1234567890
时间: 2026-07-15 15:00:00
```

---

### 2.3 生产状态人工调整通知

**事件类型**：`produce_status_changed_manual`

**触发条件**：用户手动修改订单生产状态

**接收者**：
- 订单创建者（`order.created_by`）
- 所有老板端用户（`role == "boss"`）

**标题模板**：
```
订单生产状态人工调整
```

**内容模板**：
```
用户{操作人姓名}手动将订单【{order_id}】生产状态由{原状态}修改为{新状态}
```

**状态映射**：

| 状态值 | 显示名称 |
|--------|---------|
| `unproduce` | 未生产 |
| `producing` | 生产中 |
| `produced` | 生产完成 |

**示例**：
```
用户张三手动将订单【100120260715252445123@qq.com12345】生产状态由未生产修改为生产中
```

---

### 2.4 订单生产进度更新通知（工厂上传图片触发）

**事件类型**：`produce_status_changed_factory_image`

**触发条件**：工厂端上传生产实拍图片，系统自动将生产状态更新为"生产中"

**接收者**：
- 订单创建者（`order.created_by`）
- 所有工厂端用户（`role == "factory"`）
- 所有老板端用户（`role == "boss"`）

**标题模板**：
```
订单生产进度更新
```

**内容模板**：
```
订单【{order_id}】已上传生产实拍，当前生产状态更新为：{新状态}
```

**示例**：
```
订单【100120260715252445123@qq.com12345】已上传生产实拍，当前生产状态更新为：生产中
```

---

### 2.5 订单生产全部完成通知（发货触发）

**事件类型**：`produce_status_changed_shipping`

**触发条件**：发货端将订单状态更新为"已发货"或"虚拟发货"，系统自动将生产状态固定为"生产完成"

**接收者**：
- 订单创建者（`order.created_by`）
- 所有工厂端用户（`role == "factory"`）
- 所有老板端用户（`role == "boss"`）

**标题模板**：
```
订单生产全部完成
```

**内容模板**：
```
订单【{order_id}】已完成发货，生产流程结束，生产状态固定为{新状态}
```

**示例**：
```
订单【100120260715252445123@qq.com12345】已完成发货，生产流程结束，生产状态固定为生产完成
```

---

## 三、前端展示样式

### 3.1 通知列表展示

| 字段 | 样式 | 说明 |
|------|------|------|
| 图标 | 圆形渐变背景 | 紫色渐变，48x48px |
| 标题 | 加粗黑色字体 | font-weight: 600, color: #333 |
| 时间 | 灰色小字体 | font-size: 12px, color: #999 |
| 内容 | 普通灰色字体 | font-size: 14px, line-height: 1.6 |
| 订单号 | 灰色小字体 | font-size: 12px, color: #666 |
| 状态标签 | 彩色圆角标签 | 已读：绿色，未读：黄色 |

### 3.2 未读样式

未读通知项背景色：`#fff3e6`（浅橙色）

### 3.3 图标映射

| 事件类型 | 使用图标 | 图标组件 |
|---------|---------|---------|
| `image_uploaded` | 🖼️ | Picture |
| `order_shipped` | 🚐 | Van |
| 其他 | 🔔 | Bell |

---

## 四、通知发送规则

### 4.1 去重规则

系统会检查用户是否已有相同订单、相同事件类型的通知（包括已读和未读），若存在则跳过重复发送。

### 4.2 时间格式

所有时间使用北京时区（UTC+8），格式：`YYYY-MM-DD HH:MM:SS`

---

## 五、数据库模型字段

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `id` | Integer | 主键 |
| `recipient_username` | String | 接收者用户名 |
| `order_id` | String | 关联订单ID |
| `event_type` | String | 事件类型 |
| `title` | String | 通知标题 |
| `content` | Text | 通知内容 |
| `is_read` | Boolean | 是否已读 |
| `read_at` | DateTime | 阅读时间 |
| `created_at` | DateTime | 创建时间 |

---

**文档更新时间**：2026-07-15
