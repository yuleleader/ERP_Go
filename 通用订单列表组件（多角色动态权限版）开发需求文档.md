# 通用订单列表组件（多角色动态权限版）开发需求文档
\- 编制时间：2026\-07\-11 11:19:11
\- 最后更新时间：2026\-08\-10
## 一、开发方案核心方案（既定最优方案）

**采用【单套通用订单列表组件 + 基于系统已有角色动态权限控制】方案**

不区分多角色做多套列表页面，统一封装一个全局通用订单列表组件，支持系统多处页面嵌入复用。组件自动识别当前登录用户的系统角色（老板端/销售端/发货端/工厂端），动态适配：数据权限、列表展示字段、操作按钮、编辑权限、专属上传功能。

核心优势：代码唯一、统一维护、零冗余、后续迭代只需改一处，彻底避免多页面数据不一致、权限混乱、维护成本高的问题。

## 二、组件基础特性

1. 组件形式：全局公共封装组件，支持系统任意页面快速嵌入调用，一处开发、多处复用。

2. 权限依据：完全基于系统现有四大角色身份做自动适配，无需新增角色配置。

3. 权限校验原则：**前端控制UI展示，后端做所有数据、操作、权限的最终校验，杜绝越权操作**。

4. 基础展示项（全角色通用固定前置展示）：每行数据最左侧固定展示，顺序为：**序号**（二维码列前）→ 订单二维码（复用系统已有成熟生成逻辑）→ 订单创建时上传的商品图片。

5. 关键词搜索：支持 **追溯码（=订单号 order_id）**、商品名称模糊检索（后端 `keyword` 参数同时匹配 order_id / product_name / platform_order_no / sales_amount）。

6. 路由参数联动：组件监听 `route.query` 变化（如工作台卡片点击跳转 `/orders?shipping_status=xxx`），自动重置筛选并刷新数据——解决 keep-alive 缓存下重复跳转不刷新的问题。

## 三、系统四大角色定义

复用系统已配置完成的四类账号角色：

1. 老板端

2. 销售端（订单创建主体）

3. 发货端

4. 工厂端

## 四、各角色数据权限规则（后端核心控制）

数据过滤全部由后端接口实现，前端仅做展示，防止越权查数据。

1. **销售端**：仅查询、展示当前登录销售本人创建的订单，无法查看其他销售的订单数据。

2. **老板端**：无数据限制，查看系统内所有账号创建的全量订单数据。

3. **发货端**：可查看系统全部订单数据。

4. **工厂端**：可查看系统全部订单数据。

## 五、各角色列表字段展示规则（动态列显隐）

### 1. 老板端、销售端（完整全字段展示）

展示顺序：序号 → 订单二维码 → 商品图片（首图） → 平台订单号 → 网店商品名称 → 销售金额 → 提成金额 → 订单状态 → 下单时间 → **已下单时长** → 创建人 → 发货时间

### 2. 发货端、工厂端（精简字段展示）

仅展示：序号、订单二维码、商品图片、平台订单号、网店商品名称、下单时间、**已下单时长**。

隐藏字段：销售金额、提成金额、订单状态、创建人、发货时间等所有无关、敏感字段。**已下单时长为全局公共字段，所有角色不隐藏、默认展示**。

## 六、各角色操作权限规则（按钮显隐+编辑权限控制）

### 1. 销售端（订单创建人）

可用操作：查看、编辑、删除

特殊删除规则：

① 仅订单创建人本人拥有删除权限，其他任何角色无删除权限；

② 点击删除弹出校验弹窗，必须输入**当前登录账号的账号+登录密码**；

③ 后端校验账号密码匹配通过后方可删除，校验失败禁止操作并提示；

④ 可编辑订单全部可编辑基础字段，无字段锁定限制。

### 2. 老板端

可用操作：查看、编辑

权限说明：可查看所有订单的完整信息，**无删除权限**。

### 3. 发货端

可用操作：查看、编辑（受限编辑）

编辑权限严格限制：

① 仅允许编辑【订单发货状态、物流相关信息（物流公司、物流单号等）】；

② 订单商品名称、金额、提成、创建人、下单时间等所有其他字段全部置灰只读，禁止修改；

③ 专属功能：支持上传**发货凭证图片**，图片与对应订单绑定归档，可在订单详情页预览查看；

④ 无订单删除权限。

### 4. 工厂端

可用操作：查看、编辑（仅限自身上传内容）、上传生产进度图片（专属功能）

权限说明：

① 订单基础信息全程只读：无任何订单原有信息（商品、金额、物流、状态等）的编辑权限、无订单删除权限；

② 专属编辑&上传权限：仅支持**上传、删除、替换自己提交的生产进度图片**，图片与对应订单绑定归档，可在订单详情页预览查看，该上传、编辑、删除操作仅工厂端角色可见、可用，后端单独鉴权；

③ 权限兜底：除自身上传的生产进度图片可操作外，订单所有原生数据均为只读展示，无法触发任何其他修改、删除类操作。

## 七、已下单时长字段规则（实际实现）

### 7.1 数据库字段

字段名：`order_days`（订单滞留时长）

- 类型：INTEGER（整数）
- 单位：天
- 默认值：0

### 7.2 计算逻辑

- 取自然日（仅年月日，忽略时分秒）
- 订单创建时间和当前时间均归零至当日0点
- 计算公式：`order_days = 当前自然日 - 订单创建自然日`
- 结果为纯整数，无小数

### 7.3 更新机制

- 订单新增成功时，按当日自然日初始化写入时长
- 每次查询订单列表时，后端自动以最新服务器自然日更新该字段

### 7.4 展示逻辑

- 数据库存储整数天数，前端直接原样展示（如：1天、2天、5天）
- 支持全局正向/倒序排序，默认倒序（滞留最久的订单置顶）

## 八、组件通用硬性规范

1. 高复用性：组件独立封装，嵌入系统任意页面均可自动适配当前角色权限，无需单独修改代码。

2. 双向权限兜底：前端控制UI显隐、字段只读、按钮禁用；后端对数据查询、编辑、删除、上传操作做二次鉴权，防止抓包越权。

3. 字段锁定效果：受限角色的不可编辑字段，页面置灰、禁止输入、禁止修改。

4. 逻辑统一：二维码渲染、图片展示、删除密码校验、数据过滤逻辑全局唯一，无差异化冗余代码。

5. 已下单时长字段底层规则（数据库物理存储方案）：

字段定义：数据库新增**订单滞留时长（纯数字整型字段，单位：天）**物理字段，永久入库存储，而非前端/接口实时动态计算，以整数天数维度保障排序精准、稳定、可持久化，排序效率远高于时间戳、字符串格式。

计算逻辑：系统取值规则统一取**自然日（仅取年月日、忽略时分秒）**，下单时间、服务器当前时间均归零至当日0点，计算差值：**滞留时长 = 服务器当前自然日 - 订单下单自然日**，结果为纯整数（无小数），例如：间隔1自然日=1、间隔3自然日=3，彻底杜绝0.几天的情况。

更新机制：① 订单新增成功时，按当日自然日初始化写入时长；② 每次任意用户查询、刷新订单列表时，后端以最新服务器自然日，自动同步更新该字段最新整数天数，保证数据永远为最新滞留自然天数。

展示逻辑：数据库存储整数天数，前端直接原样展示**精准自然天数**（如：1天、2天、5天），无小数、无小时秒数，完全贴合按自然日统计滞留时长的业务需求。

排序能力：该数据库字段支持**全局正向/倒序排序**，默认倒序排序（时长最大、滞留最久的订单置顶），优先暴露超期未发货订单，方便业务跟进清单。

权限展示：该字段为全局通用核心字段，**四大角色全部默认展示、不可隐藏**，是全角色统一的订单滞留监控核心字段。

## 九、整体权限总结对照表

|角色|数据范围|展示字段|查看|编辑权限|删除|专属功能|
|---|---|---|---|---|---|---|
|销售端|仅本人创建订单|全字段|✅|可编辑全部字段|✅（需账号密码校验）|无|
|老板端|全部订单|全字段|✅|可编辑全部字段|❌|无|
|发货端|全部订单|精简字段|✅|仅可改发货状态、物流信息|❌|上传发货凭证图|
|工厂端|全部订单|精简字段（含通用时长字段）|✅|仅可编辑/操作自身上传的生产进度图，订单信息只读|❌|上传生产进度图|

## 十、实际实现文件

### 10.1 后端API

- [orders.py](file:///d:/demo/完整版订单管理系统/backend/app/api/orders.py)：订单管理接口，包含数据权限过滤、订单滞留时长计算更新
- [models.py](file:///d:/demo/完整版订单管理系统/backend/app/models/models.py)：数据库模型，包含order_days字段定义

### 10.2 前端组件

- [UniversalOrderList.vue](file:///d:/demo/完整版订单管理系统/frontend/src/modules/Order/components/UniversalOrderList.vue)：通用订单列表组件，动态权限控制
- [Orders.vue](file:///d:/demo/完整版订单管理系统/frontend/src/modules/Order/views/Orders.vue)：订单管理页面，引用通用组件
- [orderService.js](file:///d:/demo/完整版订单管理系统/frontend/src/modules/Order/services/orderService.js)：订单业务逻辑层

> （注：部分内容可能由 AI 生成）
