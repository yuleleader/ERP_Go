# 2026-07-23 工作日志

## 交付前清理（用户确认方案A：全量清空业务数据）
- **备份**：`backend/data/backup/pre_delivery_20260723_142746/`
  - `order_system_full.db`（SQLite backup 接口完整快照，校验 orders=5/users=5）
  - `images_bak/`（24 个图片文件）
- **删除文件（10 个）**：`_tz2.py`、`backend/test_*.py`×5、`backend/check_db.py`、`backend/check_logs_count.py`、`backend/add_detected_country.py`（一次性迁移已执行过）、`backend/app.db`（0 字节误生成）
- **保留**：`启动器.py`、`数据库初始化.py`、`backend/run_server.py`、`backend/migrations/`
- **数据库清空**（已 VACUUM，重置自增序列）：orders(5)/shops(1)/products(1)/images(12)/notifications(12)/operation_logs(58)/shop_withdraw_record(1)/log_cleanup_records(1) 全清零；users 删除 4 个非管理员账号（2001孟祥蕾/2002高圆圆/3001工厂/4001发货，用户确认删除）
- **保留数据**：管理员 `1001 系统管理员(boss)`、logistics_companies(1)、system_settings(8)
- **图片物理文件**：`data/images/official/*`、`data/images/temp/*` 已清空（目录结构保留）
- **后端**：以交付状态重启（PID 23088），`/health` healthy，启动无报错
- 前一轮遗留确认：AccountWithdrawal.vue 重复 formatDate/formatDateTime 已删，前端 build EXIT=0；时区统一（beijing_now + strftime '+08:00' server_default + format.js 权威化）已全部生效

## 关键路径
- 真实数据库：`backend/data/db/order_system.db`（`backend/app.db` 曾是误生成空文件，已删）
- 后端启动：`python -m uvicorn app.main:app --host 0.0.0.0 --port 8000`（Python: C:/Users/Administrator/AppData/Local/Programs/Python/Python313/python.exe）
