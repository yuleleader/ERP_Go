# 电商产销协同管理系统 开发文档说明
# 最后更新时间 2026-08-11 09:30:00
## 1 项目概述

### 1.1 项目背景

搭建轻量化内网电商产销协同管理平台，打通销售端、工厂端、发货端、管理端（老板端）业务链路，替代线下表格管理，实现网店自主管理、订单统一录入、唯一追溯码+二维码溯源、工厂统一打印、发货全流程维护、图片凭证存档、操作全程留痕、销售提成可视化，提升订单流转与发货管控效率，确保数据安全可控。

### 1.2 业务目标

1. 销售端：自主维护所属网店，标准化录入订单信息，上传产品图片，追踪个人订单状态，维护订单发货状态，查看个人订单提成金额及计算逻辑。
2. 工厂端：查看所有状态下的订单（隐藏销售金额），拥有唯一二维码打印权限，配合订单流转，可上传图片告知生产任务，确保生产环节能全面对接所有订单需求。
3. 发货端：查看自己关联操作的订单（隐藏销售金额），全权维护自己关联订单的发货状态、物流单号、物流公司信息、发货凭证，确保发货流程可追溯。
4. 老板端：全数据管控，负责账号权限管理、数据备份、日志审计、销售端提成比例配置，掌握全局业务数据，可操作软件内所有功能。
5. 财务模块：包含工资结算（提成结算）和账户提现功能，支持按发货时间统计提成、提现记录管理。

### 1.3 技术架构

| 层级 | 技术选型 | 版本要求 | 说明 |
| ----- | ------- | -------- | ---- |
| 前端 | Vue3 + Element Plus | Vue 3.3+、Element Plus 2.3+ | 适配电脑端浏览器，支持响应式布局 |
| 后端 | Python FastAPI + Uvicorn | Python 3.10+、FastAPI 0.100+ | 轻量高效，支持RESTful API |
| 数据库 | SQLite 3 | 3.35+ | 单文件存储，免安装服务，适配内网轻量化部署 |
| 图片存储 | 本地文件目录归档 | - | 按固定路径存储，分临时目录、正式目录 |
| 认证方式 | Token令牌登录鉴权 | - | Token有效期可配置 |

## 2 项目目录树图

```
d:\demo\完整版订单管理系统\
├── backend\                           # Python FastAPI 后端项目
│   ├── app\                          # 应用主目录
│   │   ├── api\                     # API路由模块
│   │   │   ├── __init__.py
│   │   │   ├── auth.py              # 认证接口（登录、注册、修改密码）
│   │   │   ├── users.py            # 用户管理接口
│   │   │   ├── shops.py            # 网店管理接口
│   │   │   ├── logistics.py        # 物流公司管理接口
│   │   │   ├── orders.py           # 订单管理接口
│   │   │   ├── images.py           # 图片管理接口
│   │   │   ├── statistics.py       # 数据统计接口
│   │   │   ├── logs.py             # 日志管理接口
│   │   │   ├── products.py         # 商品管理接口
│   │   │   ├── dashboard.py        # 智慧大屏接口
│   │   │   ├── notifications.py    # 通知管理接口
│   │   │   ├── commission_settlement.py  # 提成结算接口
│   │   │   └── withdraw.py               # 账户提现接口
│   │   ├── core\                   # 核心配置模块
│   │   │   ├── __init__.py
│   │   │   ├── config.py          # 系统配置（路径、数据库、JWT等）
│   │   │   ├── database.py        # 数据库连接配置（含连接池）
│   │   │   └── security.py        # 安全工具（密码哈希、JWT）
│   │   ├── models\                # 数据库模型
│   │   │   ├── __init__.py
│   │   │   └── models.py          # SQLAlchemy模型定义（12张表）
│   │   ├── schemas\               # Pydantic schemas
│   │   │   ├── __init__.py
│   │   │   ├── logistics.py      # 物流公司schema
│   │   │   └── schemas.py        # 请求/响应模型
│   │   ├── services\             # 业务服务层
│   │   │   ├── __init__.py
│   │   │   ├── log_cleanup.py    # 日志清理服务
│   │   │   ├── notification_service.py  # 通知服务
│   │   │   └── scheduler.py      # 定时任务调度器
│   │   ├── utils\                 # 工具函数
│   │   │   ├── __init__.py
│   │   │   └── order_id_generator.py  # 订单号生成器
│   │   └── main.py               # FastAPI应用入口
│   ├── data\                     # 数据存储目录
│   │   ├── db\                  # SQLite数据库文件
│   │   ├── images\              # 图片存储目录
│   │   │   ├── temp\           # 临时图片目录
│   │   │   └── official\        # 正式图片目录（按订单号分层）
│   │   ├── backup\             # 数据库备份文件目录（启动器备份/还原生成）
│   │   └── launcher_config.json # 启动器本地配置（自动备份计划等，已 gitignore）
│   ├── migrations\              # 数据库迁移脚本
│   ├── mobile\                 # 手机端轻量作业页（纯静态，同源托管于 /m/）
│   │   ├── index.html          # 手机端单页：登录/扫码/订单详情/按角色改单/图片上传
│   │   ├── jsQR.js             # 浏览器端二维码解码库（本地内置，无需外网）
│   │   └── demo_qr.png         # 测试用系统订单二维码（编码订单号）
│   ├── requirements.txt         # Python依赖
│   ├── run_server.py            # 服务启动脚本
│   ├── start_backend.bat        # 批处理启动
│   ├── start_backend.ps1        # PowerShell启动
│   ├── test_login.py            # 登录测试
│   ├── test_pagination.py       # 分页测试
│   ├── test_logs_pagination.py  # 日志分页测试
│   ├── test_password.py         # 密码测试
│   ├── check_db.py              # 数据库检查
│   └── check_logs_count.py      # 日志数量检查
├── frontend\                      # Vue 3 前端项目
│   ├── dist\                    # 构建产物目录
│   ├── src\                     # 源代码目录
│   │   ├── api\                # API请求模块
│   │   │   ├── baseUrl.js      # 环境域名配置
│   │   │   ├── request.js      # Axios实例封装
│   │   │   ├── index.js        # API统一导出入口
│   │   │   ├── auth.js         # 认证相关API
│   │   │   ├── user.js         # 用户管理API
│   │   │   ├── shop.js         # 网店管理API
│   │   │   ├── logistics.js    # 物流公司API
│   │   │   ├── order.js        # 订单管理API
│   │   │   ├── statistics.js   # 数据统计API
│   │   │   ├── image.js        # 图片管理API
│   │   │   ├── product.js      # 商品管理API
│   │   │   ├── logs.js         # 日志管理API
│   │   │   ├── notification.js # 通知管理API
│   │   │   ├── exchange.js     # 汇率API
│   │   │   ├── dashboard.js    # 智慧大屏API
│   │   │   ├── commission_settlement.js  # 提成结算API
│   │   │   └── withdraw.js               # 账户提现API
│   │   ├── modules\            # 业务模块目录
│   │   │   ├── Common\         # 公共模块
│   │   │   │   └── components\
│   │   │   │       ├── ExchangeCalculator.vue   # 汇率计算器
│   │   │   │       ├── OrderOverview.vue       # 订单概览
│   │   │   │       ├── SalesPerformance.vue    # 销售业绩
│   │   │   │       ├── FinanceStatistics.vue   # 财务统计
│   │   │   │       ├── SalesRanking.vue        # 销售排行
│   │   │   │       ├── ProductRanking.vue      # 商品排行
│   │   │   │       ├── SystemControls.vue      # 系统控制
│   │   │   │       └── world.json             # 世界地图数据
│   │   │   ├── Order\          # 订单管理模块
│   │   │   │   ├── views\
│   │   │   │   │   └── Orders.vue
│   │   │   │   ├── components\
│   │   │   │   │   └── UniversalOrderList.vue  # 通用订单列表组件
│   │   │   │   └── services\
│   │   │   │       └── orderService.js
│   │   │   ├── Product\        # 商品管理模块
│   │   │   │   └── services\
│   │   │   │       └── productService.js
│   │   │   ├── Shop\           # 网店管理模块
│   │   │   │   └── services\
│   │   │   │       └── shopService.js
│   │   │   ├── Logistics\      # 物流管理模块
│   │   │   │   └── services\
│   │   │   │       └── logisticsService.js
│   │   │   ├── Image\          # 图片管理模块
│   │   │   │   └── services\
│   │   │   │       └── imageService.js
│   │   │   ├── Log\            # 日志管理模块
│   │   │   │   └── services\
│   │   │   │       └── logService.js
│   │   │   ├── Statistics\     # 数据统计模块
│   │   │   │   └── services\
│   │   │   │       └── statisticsService.js
│   │   │   ├── Settings\       # 系统设置模块
│   │   │   │   └── services\
│   │   │   │       └── settingsService.js
│   │   │   ├── User\           # 用户管理模块
│   │   │   │   └── services\
│   │   │   │       └── userService.js
│   │   │   └── Finance\        # 财务模块
│   │   │       └── views\
│   │   │           ├── CommissionSettlement.vue  # 工资结算
│   │   │           └── AccountWithdrawal.vue     # 账户提现
│   │   ├── router\             # 路由配置
│   │   │   └── index.js        # Vue Router配置（含权限守卫）
│   │   ├── store\              # Pinia状态管理
│   │   │   ├── user.js         # 用户状态管理
│   │   │   └── users.js        # 用户列表状态管理
│   │   ├── utils\              # 工具函数
│   │   │   ├── request.js      # Axios封装
│   │   │   ├── format.js       # 格式化函数
│   │   │   ├── countryData.js  # 国家数据（地图定位）
│   │   │   ├── addressParser.js # 地址解析器
│   │   │   └── exchangeRequest.js # 汇率请求工具
│   │   ├── styles\             # 样式目录
│   │   │   └── dark-theme.scss # 深色主题样式
│   │   ├── views\              # 页面组件（根页面）
│   │   │   ├── Login.vue       # 登录页面
│   │   │   ├── Layout.vue      # 主布局（侧边栏+汇率计算器）
│   │   │   ├── Dashboard.vue   # 工作台（角色路由）
│   │   │   ├── BossDashboard.vue # 老板端工作台
│   │   │   ├── SalesDashboard.vue # 销售端工作台
│   │   │   ├── FactoryDashboard.vue # 工厂端工作台
│   │   │   ├── ShippingDashboard.vue # 发货端工作台
│   │   │   ├── Users.vue       # 用户管理
│   │   │   ├── Shops.vue       # 网店管理
│   │   │   ├── Orders.vue      # 订单管理（模块入口）
│   │   │   ├── Products.vue    # 商品管理
│   │   │   ├── Images.vue      # 图片管理
│   │   │   ├── Logistics.vue   # 物流公司管理
│   │   │   ├── Statistics.vue  # 数据统计
│   │   │   ├── Logs.vue        # 日志管理
│   │   │   ├── Settings.vue    # 系统设置
│   │   │   ├── Notifications.vue # 站内装他信
│   │   │   └── SmartDashboard.vue # 智慧大屏
│   │   ├── App.vue             # 根组件
│   │   ├── main.js             # 应用入口
│   │   └── style.css           # 全局样式
│   ├── index.html              # HTML入口
│   ├── package.json            # npm依赖配置
│   └── vite.config.js          # Vite配置
├── 启动器.py                       # 可视化启动器（推荐使用）
├── 一键启动.bat                    # 批处理启动脚本
├── 数据库初始化.py                  # 数据库初始化脚本
└── 开发文档说明.md              # 本文档
```

## 3 数据库设计

### 3.1 数据库ER关系图

```
                           ┌─────────────────┐
                           │    用户表        │
                           │   (users)       │
                           ├─────────────────┤
                           │ id (PK)         │
                           │ username        │
                           │ password_hash   │
                           │ real_name       │
                           │ role            │
                           │ commission_rate │
                           │ is_active       │
                           │ created_at      │
                           └────────┬────────┘
                                    │
         ┌──────────────────────────┼──────────────────────────┐
         │                          │                          │
         ▼                          ▼                          ▼
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   登录日志表     │     │   操作日志表     │     │    通知表        │
│ (login_logs)    │     │(operation_logs)│     │(notifications)  │
├─────────────────┤     ├─────────────────┤     ├─────────────────┤
│ id (PK)         │     │ id (PK)         │     │ id (PK)         │
│ username        │     │ username (FK)   │     │ recipient_...   │
│ login_time      │     │ operation_type  │     │ order_id        │
│ ip_address      │     │ operation_desc  │     │ event_type      │
│ status          │     │ created_at      │     │ title           │
└─────────────────┘     └─────────────────┘     │ content         │
                                                │ is_read         │
                                                │ created_at      │
                                                └─────────────────┘
                                    │
                                    ▼
                           ┌─────────────────┐
                           │    订单表        │
                           │   (orders)      │
                           ├─────────────────┤
                           │ id (PK)         │
                           │ order_id (UK)   │
         ┌─────────────────┤ creator (FK)    │─────────────────┐
         │                 │ shop_id (FK)    │                 │
         │                 │ product_name    │◄────────────────┤
         │                 │ sales_amount    │                 │
         │                 │ commission_amt  │                 │
         │                 │ shipping_status │                 │
         │                 │ logistics_...   │                 │
         │                 │ shipping_time   │                 │
         │                 └─────────────────┘                 │
         │                          │                          │
         │                          │                          │
         ▼                          ▼                          ▼
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│    网店表        │     │    图片表        │     │    商品表        │
│   (shops)       │     │   (images)      │     │  (products)     │
├─────────────────┤     ├─────────────────┤     ├─────────────────┤
│ id (PK)         │     │ id (PK)         │     │ id (PK)         │
│ shop_id (UK)   │     │ order_id (FK)   │     │ product_code(UK)│
│ shop_name      │     │ image_type      │     │ product_name    │
│ status         │     │ image_url       │     │ product_remark  │
│ created_by     │     │ uploaded_by     │     │ status          │
│ created_at     │     │ created_at      │     │ created_by      │
└─────────────────┘     └─────────────────┘     │ created_at      │
                                                │ updated_at      │
                                                └─────────────────┘
                                    │
                    ┌───────────────┼───────────────┐
                    ▼                               ▼
           ┌─────────────────┐              ┌─────────────────┐
           │  提现记录表      │              │  系统设置表      │
           │(shop_withdraw_  │              │(system_settings)│
           │  record)        │              ├─────────────────┤
           ├─────────────────┤              │ id (PK)         │
           │ id (PK)         │              │ key (UK)        │
           │ shop_id (FK)    │              │ value           │
           │ withdraw_date   │              │ description     │
           │ withdraw_amount │              │ created_at      │
           │ create_time     │              └─────────────────┘
           │ update_time     │
           └─────────────────┘
```

**关系说明**：
- **用户表** 与 **订单表**：1:N（一个用户可创建多个订单）
- **用户表** 与 **操作日志表**：1:N（一个用户可产生多条操作日志）
- **用户表** 与 **登录日志表**：1:N（一个用户可产生多条登录记录）
- **用户表** 与 **通知表**：1:N（一个用户可接收多条通知）
- **网店表** 与 **订单表**：1:N（一个网店可关联多个订单）
- **订单表** 与 **图片表**：1:N（一个订单可关联多张图片）
- **商品表** 与 **订单表**：1:N（一个商品可出现在多个订单中，通过product_name字段关联）
- **网店表** 与 **提现记录表**：1:N（一个网店可关联多条提现记录）
- **用户表** 与 **提现记录表**：1:N（一个用户可创建多条提现记录）
- **订单表** 与 **通知表**：1:N（一个订单可关联多条通知）

### 3.2 数据表结构

#### 3.2.1 用户表（users）

| 字段名 | 类型 | 是否主键 | 是否必填 | 说明 |
| ----- | ----- | -------- | -------- | ---- |
| id | INTEGER | 是 | 是 | 自增主键 |
| username | TEXT | 否 | 是 | 登录账号，全局唯一 |
| password_hash | TEXT | 否 | 是 | 密码哈希存储 |
| real_name | TEXT | 否 | 否 | 真实姓名 |
| role | TEXT | 否 | 是 | 角色类型：boss/sales/factory/shipping |
| commission_rate | INTEGER | 否 | 否 | 提成比例（仅sales角色，1-100） |
| is_active | INTEGER | 否 | 是 | 账号状态：1启用/0禁用，默认1 |
| created_at | TIMESTAMP | 否 | 是 | 创建时间，自动生成 |
| updated_at | TIMESTAMP | 否 | 是 | 更新时间，自动更新 |

#### 3.2.2 网店表（shops）

| 字段名 | 类型 | 是否主键 | 是否必填 | 说明 |
| ----- | ----- | -------- | -------- | ---- |
| id | INTEGER | 是 | 是 | 自增主键 |
| shop_id | TEXT | 否 | 是 | 网店唯一标识（店铺名称明文），全局唯一 |
| shop_name | TEXT | 否 | 是 | 店铺名称，不可重复 |
| shop_account | TEXT | 否 | 是 | 店铺账号 |
| status | TEXT | 否 | 是 | 网店状态：normal/closed，默认normal |
| creator | TEXT | 否 | 是 | 创建人用户名，关联users表username |
| create_time | TIMESTAMP | 否 | 是 | 创建时间，自动生成 |
| update_time | TIMESTAMP | 否 | 是 | 更新时间，自动更新 |

#### 3.2.3 物流公司表（logistics_companies）

| 字段名 | 类型 | 是否主键 | 是否必填 | 说明 |
| ----- | ----- | -------- | -------- | ---- |
| id | INTEGER | 是 | 是 | 自增主键 |
| company_code | TEXT | 否 | 是 | 物流公司代码，唯一 |
| company_name | TEXT | 否 | 是 | 物流公司名称，唯一 |
| contact_person | TEXT | 否 | 否 | 联系人 |
| contact_phone | TEXT | 否 | 否 | 联系电话 |
| status | TEXT | 否 | 是 | 状态：active/inactive，默认active |
| created_by | TEXT | 否 | 是 | 创建人用户名，关联user表username |
| created_at | TIMESTAMP | 否 | 是 | 创建时间，自动生成 |
| updated_at | TIMESTAMP | 否 | 是 | 更新时间，自动更新 |

#### 3.2.4 订单表（orders）

| 字段名 | 类型 | 是否主键 | 是否必填 | 说明 |
| ----- | ----- | -------- | -------- | ---- |
| id | INTEGER | 是 | 是 | 自增主键 |
| order_id | TEXT | 否 | 是 | 追溯码，全局唯一，按固定规则生成 |
| shop_id | TEXT | 否 | 是 | 关联网店表shop_id |
| product_name | TEXT | 否 | 否 | 商品名称 |
| platform_order_no | TEXT | 否 | 是 | 平台原始订单号，唯一 |
| sales_amount | TEXT | 否 | 是 | 销售金额，保留2位小数 |
| shipping_status | TEXT | 否 | 是 | 发货状态：pending/shipped/virtual/refunded，默认pending（refunded=已退货/退款） |
| logistics_company | TEXT | 否 | 否 | 物流公司名称 |
| logistics_no | TEXT | 否 | 否 | 物流单号 |
| shipping_operator | TEXT | 否 | 否 | 发货操作人 |
| shipping_time | TIMESTAMP | 否 | 否 | 发货操作时间，状态变更为已发货时自动填充，支持手动编辑 |
| receiver_address | TEXT | 否 | 是 | 收货地址 |
| remark | TEXT | 否 | 否 | 订单备注 |
| commission_rate | INTEGER | 否 | 否 | 提成比例 |
| commission_amount | TEXT | 否 | 否 | 提成金额，自动计算 |
| created_by | TEXT | 否 | 是 | 创建人用户名，关联user表username |
| created_at | TIMESTAMP | 否 | 是 | 创建时间，可自定义 |
| order_days | INTEGER | 否 | 否 | 订单滞留时长（单位：天） |
| commission_paid | BOOLEAN | 否 | 是 | 提成是否已发放，默认false |
| updated_at | TIMESTAMP | 否 | 是 | 更新时间，自动更新 |
| produce_status | TEXT | 否 | 是 | 生产状态：unproduce/producing/produced，默认unproduce |
| produce_status_update_at | TIMESTAMP | 否 | 否 | 生产状态最后变更时间戳 |
| produce_status_update_user | TEXT | 否 | 否 | 变更操作人标识：手动修改存储操作人用户名，系统自动变更填写system-auto |

#### 3.2.5 图片表（images）

| 字段名 | 类型 | 是否主键 | 是否必填 | 说明 |
| ----- | ----- | -------- | -------- | ---- |
| id | INTEGER | 是 | 是 | 自增主键 |
| temp_id | TEXT | 否 | 否 | 临时图片ID |
| order_id | TEXT | 否 | 否 | 关联订单号，临时图片时为null |
| image_type | TEXT | 否 | 否 | 图片类型：product/shipping/virtual/production（兼容旧字段，归档与分类展示以 layer 为准） |
| image_url | TEXT | 否 | 否 | 图片存储路径 |
| thumbnail_url | TEXT | 否 | 否 | 缩略图路径 |
| image_hash | TEXT | 否 | 否 | 图片MD5哈希值 |
| is_main | INTEGER | 否 | 否 | 是否主图：1是/0否，默认0 |
| uploaded_by | TEXT | 否 | 否 | 上传人用户名，关联users表username |
| is_locked | INTEGER | 否 | 否 | 是否锁定：1是/0否，默认0 |
| layer | TEXT | 否 | 否 | 图片层级：sales/factory/shipping，默认sales |
| created_at | TIMESTAMP | 否 | 是 | 创建时间，自动生成 |
| updated_at | TIMESTAMP | 否 | 是 | 更新时间，自动更新 |

#### 3.2.6 操作日志表（operation_logs）

| 字段名 | 类型 | 是否主键 | 是否必填 | 说明 |
| ----- | ----- | -------- | -------- | ---- |
| id | INTEGER | 是 | 是 | 自增主键 |
| user_id | INTEGER | 否 | 否 | 操作人ID |
| username | TEXT | 否 | 否 | 操作人用户名 |
| operation_type | TEXT | 否 | 是 | 操作类型 |
| operation_content | TEXT | 否 | 否 | 操作内容 |
| ip_address | TEXT | 否 | 否 | IP地址 |
| user_agent | TEXT | 否 | 否 | 用户代理 |
| created_at | TIMESTAMP | 否 | 是 | 操作时间，自动生成 |

#### 3.2.7 登录日志表（login_logs）

| 字段名 | 类型 | 是否主键 | 是否必填 | 说明 |
| ----- | ----- | -------- | -------- | ---- |
| id | INTEGER | 是 | 是 | 自增主键 |
| username | TEXT | 否 | 是 | 登录账号 |
| login_time | TIMESTAMP | 否 | 是 | 登录时间 |
| ip_address | TEXT | 否 | 否 | IP地址 |
| user_agent | TEXT | 否 | 否 | 用户代理 |
| status | TEXT | 否 | 是 | 登录状态：success/failed，默认success |

#### 3.2.8 商品表（products）

| 字段名 | 类型 | 是否主键 | 是否必填 | 说明 |
| ----- | ----- | -------- | -------- | ---- |
| id | INTEGER | 是 | 是 | 自增主键 |
| product_code | TEXT | 否 | 是 | 商品唯一标识码（UUID v4格式：PRD-XXXXXXXX），全局唯一 |
| product_name | TEXT | 否 | 是 | 商品名称，长度限制2-100字符，与订单表product_name字段关联 |
| product_remark | TEXT | 否 | 否 | 商品备注，支持富文本格式，最大长度500字符 |
| status | TEXT | 否 | 是 | 商品状态：active(启用)/inactive(停用)，默认active |
| created_by | TEXT | 否 | 是 | 创建人用户名，关联user表username |
| created_at | TIMESTAMP | 否 | 是 | 创建时间，自动生成 |
| updated_at | TIMESTAMP | 否 | 是 | 更新时间，自动更新 |

#### 3.2.9 提现记录表（shop_withdraw_record）

| 字段名 | 类型 | 是否主键 | 是否必填 | 说明 |
| ----- | ----- | -------- | -------- | ---- |
| id | INTEGER | 是 | 是 | 自增主键 |
| shop_id | TEXT | 否 | 是 | 关联网店表shop_id |
| withdraw_date | TEXT | 否 | 是 | 提现日期，格式YYYY-MM-DD |
| withdraw_amount | TEXT | 否 | 是 | 提现金额，保留2位小数 |
| remark | TEXT | 否 | 否 | 备注 |
| create_operator_name | TEXT | 否 | 是 | 创建人姓名 |
| create_operator_id | INTEGER | 否 | 是 | 创建人ID |
| update_operator_name | TEXT | 否 | 否 | 更新人姓名 |
| update_operator_id | INTEGER | 否 | 否 | 更新人ID |
| create_time | TIMESTAMP | 否 | 是 | 创建时间，自动生成（北京时间） |
| update_time | TIMESTAMP | 否 | 是 | 更新时间，自动更新（北京时间） |

#### 3.2.10 通知表（notifications）

| 字段名 | 类型 | 是否主键 | 是否必填 | 说明 |
| ----- | ----- | -------- | -------- | ---- |
| id | INTEGER | 是 | 是 | 自增主键 |
| recipient_username | TEXT | 否 | 是 | 接收人用户名，关联users表username，索引 |
| order_id | TEXT | 否 | 否 | 关联订单号，索引 |
| event_type | TEXT | 否 | 是 | 事件类型 |
| title | TEXT | 否 | 是 | 通知标题 |
| content | TEXT | 否 | 是 | 通知内容 |
| is_read | BOOLEAN | 否 | 是 | 是否已读，默认false |
| read_at | TIMESTAMP | 否 | 否 | 读取时间 |
| created_at | TIMESTAMP | 否 | 是 | 创建时间，自动生成 |

#### 3.2.11 日志清理记录表（log_cleanup_records）

| 字段名 | 类型 | 是否主键 | 是否必填 | 说明 |
| ----- | ----- | -------- | -------- | ---- |
| id | INTEGER | 是 | 是 | 自增主键 |
| cleanup_type | TEXT | 否 | 是 | 清理类型：operation/login/all |
| retention_days | INTEGER | 否 | 是 | 保留天数 |
| deleted_count | INTEGER | 否 | 是 | 删除数量，默认0 |
| status | TEXT | 否 | 是 | 状态：pending/success/failed |
| error_message | TEXT | 否 | 否 | 错误信息 |
| start_time | TIMESTAMP | 否 | 是 | 开始时间 |
| end_time | TIMESTAMP | 否 | 否 | 结束时间 |
| triggered_by | TEXT | 否 | 否 | 触发方式：manual/scheduled |
| operator_username | TEXT | 否 | 否 | 操作人用户名 |

#### 3.2.12 系统设置表（system_settings）

| 字段名 | 类型 | 是否主键 | 是否必填 | 说明 |
| ----- | ----- | -------- | -------- | ---- |
| id | INTEGER | 是 | 是 | 自增主键 |
| key | TEXT | 否 | 是 | 设置键，全局唯一 |
| value | TEXT | 否 | 否 | 设置值 |
| description | TEXT | 否 | 否 | 设置描述 |
| created_at | TIMESTAMP | 否 | 是 | 创建时间，自动生成 |
| updated_at | TIMESTAMP | 否 | 是 | 更新时间，自动更新 |

## 4 核心字段定义

### 4.1 订单追溯码（order_id）生成规则

- **生成格式**：`登录账号（username）+ 年月日（YYYYMMDD）+ 网店ID（shop_account）+平台订单号（platform_order_no）+ 6位随机数字`
- **示例**：zhangsan20260424淘宝XX旗舰店P20260424001123456
- **并发防重机制**：
  - 系统启动时预生成1000个6位随机号存入内存队列
  - 新增订单时从队列取号拼接
  - 若重复则重试最多3次
  - 队列剩余≤100个时自动补充

### 4.2 商品唯一标识码（product_code）生成规则

- **生成格式**：`PLU-{6位数字}`（顺序编号，从1开始递增）
- **示例**：PLU-000001、PLU-000002、PLU-000100
- **唯一性保证**：基于数据库自增ID生成，保证唯一性
- **生成时机**：商品创建时自动生成，不可修改

### 4.3 提成计算公式

- **公式**：`提成金额 = 销售金额 × 提成比例 ÷ 100`
- **取值范围**：提成比例1-100（仅销售端账号）
- **计算时机**：订单创建时、编辑销售金额时自动计算

### 4.4 图片存储方案（方案A）

- **临时目录**：`data/images/temp/`
- **正式目录**：`data/images/official/{order_id}/{layer}/`（按追溯码+层级分目录）
- **层级分类**：
  - `sales`（销售端）：商品图片
  - `factory`（工厂端）：生产进度图片
  - `shipping`（发货端）：发货凭证图片
- **清理机制**：临时图片保留24小时
- **完整性验证**：使用MD5哈希值校验

### 4.5 生产状态定义

#### 4.5.1 生产状态枚举

| 存储值 | 展示名称 | 说明 |
| ------ | -------- | ---- |
| unproduce | 未生产 | 新建订单默认状态，未开展任何生产作业 |
| producing | 生产中 | 工厂上传生产图片自动切换，代表车间正在生产 |
| produced | 生产完成 | 订单发货后自动锁定，生产流程永久结束，不存在返工、回退场景 |

#### 4.5.2 手动修改权限&锁定规则

- **手动修改权限**：全部角色（boss、sales、factory、shipping）均可手动修改订单生产状态
- **永久锁定机制**：只要订单 `shipping_status = shipped / virtual`（已发货/虚拟发货），系统永久锁定生产状态为「生产完成」；任何人手动修改为「未生产/生产中」直接拦截，弹窗提示：订单已完成发货，生产流程结束，生产状态不可调整
- **撤销发货锁定**：即使后续手动撤销发货、将 `shipping_status` 改回 `pending`，生产状态依旧保持 `produced`，不允许人工下调，不支持返工

#### 4.5.3 系统自动变更触发逻辑

**触发1：工厂端上传factory层级生产图片**
- **触发条件**：上传用户角色为factory；图片层级 layer = factory（生产实拍图）；订单当前生产状态 ≠ producing
- **执行动作**：自动更新 `produce_status = producing`；同步更新 `produce_status_update_at`、`produce_status_update_user = system-auto`；写入操作日志，标记类型：系统自动-工厂上传生产图片；推送站内通知至订单创建销售、工厂、全部老板账号
- **防抖规则**：订单已是「生产中」时，重复上传多张生产图片，不重复更新状态、不新增日志、不推送通知

**触发2：发货端更新订单为已发货/虚拟发货**
- **触发条件**：接口更新订单 `shipping_status` 从 `pending` 变为 `shipped / virtual`
- **执行动作**：强制固定 `produce_status = produced`，不受前端传入参数影响；同步更新 `produce_status_update_at`、`produce_status_update_user = system-auto`；写入操作日志，标记类型：系统自动-订单完成发货，生产流程结束；推送站内通知至订单创建销售、工厂、全部老板账号；永久锁定，后续无论是否撤销发货，生产状态固定不变

#### 4.5.4 操作日志留存规范

所有生产状态变更（手动修改、系统自动变更）强制写入 `operation_logs`，操作类型统一为 `update_produce_status`：

| 变更类型 | 日志文案格式 |
| -------- | ----------- |
| 手动修改 | 用户{操作人姓名}手动调整订单生产状态：{旧状态} → {新状态} |
| 工厂传图自动变更 | 系统自动更新：工厂上传生产图片，订单生产状态由{旧状态}变更为生产中 |
| 发货自动变更 | 系统自动锁定：订单完成发货，生产状态强制更新为生产完成，生产流程结束 |

#### 4.5.5 站内通知推送规则

| 场景 | 接收人 | 标题 | 内容 |
| ---- | ------ | ---- | ---- |
| 工厂上传图片→自动变为生产中 | 订单创建销售、当前工厂账号、所有老板账号 | 订单生产进度更新 | 订单【{order_id}】已上传生产实拍，当前生产状态更新为：生产中 |
| 订单完成发货→自动锁定为生产完成 | 订单创建销售、工厂账号、所有老板账号 | 订单生产全部完成 | 订单【{order_id}】已完成发货，生产流程结束，生产状态固定为生产完成 |
| 任意角色手动修改生产状态 | 订单创建销售、所有老板账号 | 订单生产状态人工调整 | 用户{操作人姓名}手动将订单【{order_id}】生产状态由{旧状态}修改为{新状态} |

#### 4.5.6 站内信模板样式详表

> 完整模板统计见 `站内信模板样式统计.md`，此处为摘要。系统共 6 类站内信事件，时间统一北京时间 `YYYY-MM-DD HH:MM:SS`；同订单+同事件自动去重（含已读/未读）。

| 事件类型 | 触发场景 | 接收者 | 标题 | 内容模板 |
| -------- | -------- | ------ | ---- | -------- |
| `order_created` | 销售/老板创建新订单 | 全部工厂端 | 新订单提醒 | {creator_name}创建了新订单【{order_id}】，【{product_name}】【{created_at}】订单号【{order_id}】 |
| `image_uploaded` | 工厂上传生产实拍图片 | 订单创建者 | 图片上传完成 | 订单 {order_id} 的图片已上传完成。时间: {当前时间} |
| `order_shipped` | 发货端更新为已发货/虚拟发货 | 订单创建者 | 订单已发货 | 订单 {order_id} 已发货。{物流公司: xxx}{物流单号: xxx}时间: {当前时间}（物流字段可选，有则显示） |
| `produce_status_changed_manual` | 手动修改生产状态 | 订单创建者、所有老板端 | 订单生产状态人工调整 | 用户{操作人姓名}手动将订单【{order_id}】生产状态由{原状态}修改为{新状态} |
| `produce_status_changed_factory_image` | 工厂上传生产图片自动变为生产中 | 订单创建者、所有工厂端、所有老板端 | 订单生产进度更新 | 订单【{order_id}】已上传生产实拍，当前生产状态更新为：{新状态} |
| `produce_status_changed_shipping` | 发货后自动锁定生产完成 | 订单创建者、所有工厂端、所有老板端 | 订单生产全部完成 | 订单【{order_id}】已完成发货，生产流程结束，生产状态固定为{新状态} |

**生产状态显示映射**：`unproduce`→未生产、`producing`→生产中、`produced`→生产完成。

**前端展示样式**：通知列表图标 48x48 圆形渐变背景（紫）、标题加粗黑色（font-weight:600, #333）、时间/订单号灰色小字（12px, #999/#666）、内容 14px 灰、状态标签（已读绿/未读黄）、未读项背景 `#fff3e6`；图标映射 `image_uploaded`→Picture、`order_shipped`→Van、其他→Bell。

**数据模型**：`notifications` 表（id, recipient_username, order_id, event_type, title, content, is_read, read_at, created_at）。

#### 4.5.7 全场景边界规则

| 场景 | 处理逻辑 |
| ---- | -------- |
| 新建订单 | 默认 `unproduce`（未生产），无通知推送 |
| 工厂多次上传生产图片 | 订单当前已是生产中，再次上传factory层级图片，不触发状态变更、不新增日志、不推送通知 |
| 订单已发货，人工下调状态 | 系统直接拦截，弹窗提示：该订单已完成发货，生产流程已结束，生产状态锁定为【生产完成】，无法修改 |
| 撤销发货（shipping_status改回pending） | 生产状态维持 `produced` 不变，所有角色均无法手动改回「生产中/未生产」，无返工流程 |
| 虚拟发货订单 | 虚拟发货完成后，和实体发货逻辑完全一致，自动锁定生产状态为生产完成，不可回退 |
| 批量新建订单 | 批量创建的所有订单，生产状态统一默认「未生产」，无自动状态变更 |
| 删除订单 | 删除订单时，操作日志同步记录删除瞬间对应的生产状态，完整留存追溯信息 |
| 多账号同时操作冲突 | 前端提交修改前实时校验数据库最新状态，若状态已被其他账号/系统自动变更，弹窗刷新最新状态后再操作 |

#### 4.5.8 订单详情生产进度展示与编辑

订单详情弹窗（共享组件 `OrderDetailDialog.vue`，同时被「订单列表-查看」「智慧大屏-超期订单点击订单ID」「站内信-点击订单」复用；站内信 `Notifications.vue` 内联详情弹窗逻辑一致）在右侧栏「二维码 + 发货状态」下方新增**生产进度卡片**：

- **当前状态**：以标签展示 `produce_status`——未生产（灰）/ 生产中（橙）/ 生产完成（绿）
- **更新信息**：展示 `produce_status_update_at`（更新时间）与 `produce_status_update_user`（更新人）
- **手动修改**：下拉选择新状态后自动调用 `PUT /api/orders/{order_id}` 更新；权限与第 5.1 节一致（boss/sales/factory/shipping 均可修改，发货后锁定为生产完成不可逆）

> 后端无需新增接口：Order 模型已含 `produce_status` / `produce_status_update_at` / `produce_status_update_user` 三字段，`get_order` 已返回，更新复用既有订单更新接口。

## 5 角色权限说明

| 角色 | 数据查看范围 | 核心操作权限 | 权限禁止项 |
| ----- | ----------- | ------------ | --------- |
| 老板端(boss) | 全部数据 | 所有功能、批量修改生产状态 | 无 |
| 销售端(sales) | 自己创建的网店、订单 | 订单录入、编辑、图片上传、修改生产状态 | 不可查看他人订单、不可打印二维码 |
| 工厂端(factory) | 所有订单（隐藏金额） | 查看所有订单、二维码打印、上传生产图片（自动触发生产中状态）、修改生产状态 | 不可编辑订单、不可查看金额 |
| 发货端(shipping) | 自己关联的订单（隐藏金额） | 发货状态、物流信息维护、修改生产状态、发货自动锁定生产完成 | 不可新增/编辑订单、不可查看金额 |

### 5.1 生产状态修改权限说明

| 角色 | 手动修改生产状态 | 批量修改生产状态 | 自动触发生产状态变更 |
| ----- | -------------- | --------------- | ------------------- |
| 老板端(boss) | 是 | 是 | 无 |
| 销售端(sales) | 是 | 否 | 无 |
| 工厂端(factory) | 是 | 否 | 上传生产图片自动变为生产中 |
| 发货端(shipping) | 是 | 否 | 更新发货状态自动锁定为生产完成 |

**生产状态锁定规则**：订单发货后（shipping_status = shipped / virtual），生产状态永久锁定为「生产完成」，所有角色均无法手动下调为「未生产/生产中」，不支持返工流程。

### 5.2 通用订单列表组件（多角色动态权限版）

> 详细需求与规则见 `通用订单列表组件（多角色动态权限版）开发需求文档.md`，此处为摘要。

**方案**：单套通用订单列表组件（`frontend/src/modules/Order/components/UniversalOrderList.vue`）+ 基于系统已有角色动态权限控制，一处开发、多处复用（订单管理页 `Orders.vue` 等）。组件自动识别当前登录角色，动态适配数据权限、展示字段、操作按钮、编辑权限与专属上传功能。

**权限校验原则**：前端控制 UI 展示（字段显隐、按钮禁用、字段只读置灰），后端做所有数据/操作/权限的最终校验（防止抓包越权）。

**各角色对照**：

| 角色 | 数据范围 | 展示字段 | 编辑权限 | 删除 | 专属功能 |
| ---- | ------- | ------- | ------- | ---- | ------- |
| 销售端 | 仅本人创建订单 | 全字段 | 可编辑全部字段 | ✅（需账号+密码二次校验，仅创建人） | 无 |
| 老板端 | 全部订单 | 全字段 | 可编辑全部字段 | ❌ | 无 |
| 发货端 | 全部订单 | 精简字段（隐藏金额/提成/状态/创建人等） | 仅发货状态+物流信息 | ❌ | 上传发货凭证图 |
| 工厂端 | 全部订单 | 精简字段 | 订单信息全程只读 | ❌ | 上传/删除/替换自己提交的生产进度图（后端单独鉴权） |

**列表基础展示**（全角色固定前置）：序号 → 订单二维码 → 商品图片（首图）→ 平台订单号 → 网店商品名称 → 下单时间 → 已下单时长。

**关键词搜索**：追溯码（=order_id）/ 商品名称模糊（后端 `keyword` 同时匹配 order_id / product_name / platform_order_no / sales_amount）。

**路由联动**：监听 `route.query` 变化（如工作台卡片跳转 `/orders?shipping_status=xxx`）自动重置筛选并刷新，解决 keep-alive 缓存下重复跳转不刷新的问题。

**已下单时长（order_days）字段规则**：
- 数据库物理整数字段（单位：天，默认 0），非前端实时计算——保障排序精准、稳定、可持久化。
- 计算：取自然日（仅年月日、忽略时分秒），`order_days = 服务器当前自然日 - 订单下单自然日`，纯整数无小数。
- 更新：订单新增时按当日初始化；每次查询订单列表时后端以最新自然日自动刷新。
- 展示：前端原样展示整数天数（如 1天、2天、5天）；支持全局正/倒序排序，默认倒序（滞留最久置顶）；全角色默认展示、不可隐藏。

**实现文件**：后端 `orders.py`（数据权限过滤、order_days 计算）、`models.py`（order_days 字段）；前端 `UniversalOrderList.vue`（通用组件）、`Orders.vue`（订单管理页）、`orderService.js`（业务逻辑层）。

## 6 接口规范

### 6.1 通用规范

- 请求方式：RESTful API（GET/POST/PUT/DELETE）
- 认证方式：Token令牌（Authorization: Bearer Token）
- 返回格式：统一JSON格式 `{code, message, data}`
- 状态码：200成功、400参数错误、401未登录、403权限不足、404不存在、500服务器错误

### 6.2 核心接口列表

| 模块 | 接口路径 | 方法 | 说明 |
| ---- | ------- | ---- | ---- |
| 认证 | /api/auth/login | POST | 登录 |
| 认证 | /api/auth/register | POST | 注册 |
| 认证 | /api/auth/me | GET | 获取当前用户 |
| 用户 | /api/users/ | GET | 用户列表 |
| 用户 | /api/users/ | POST | 创建用户 |
| 用户 | /api/users/{id} | PUT | 更新用户 |
| 用户 | /api/users/{id} | DELETE | 删除用户 |
| 网店 | /api/shops/ | GET | 网店列表 |
| 网店 | /api/shops/ | POST | 创建网店 |
| 物流公司 | /api/logistics/companies | GET | 物流公司列表 |
| 物流公司 | /api/logistics/companies | POST | 创建物流公司 |
| 物流公司 | /api/logistics/companies/{id} | PUT | 更新物流公司 |
| 物流公司 | /api/logistics/companies/{id} | DELETE | 删除物流公司 |
| 商品 | /api/products/ | GET | 商品列表（支持关键词搜索、状态筛选） |
| 商品 | /api/products/ | POST | 创建商品（仅boss、sales） |
| 商品 | /api/products/{product_code} | GET | 商品详情 |
| 商品 | /api/products/{product_code} | PUT | 更新商品（仅boss、sales） |
| 商品 | /api/products/{product_code} | DELETE | 删除商品（仅boss，已被订单引用的商品不允许删除） |
| 商品 | /api/products/batch-delete | POST | 批量删除商品（仅boss） |
| 商品 | /api/products/count/total | GET | 商品统计（总数、启用数、停用数） |
| 订单 | /api/orders/ | GET | 订单列表（支持平台订单号/商品名称/销售金额搜索） |
| 订单 | /api/orders/ | POST | 创建订单 |
| 订单 | /api/orders/{order_id} | GET | 订单详情 |
| 订单 | /api/orders/{order_id} | PUT | 更新订单 |
| 订单 | /api/orders/{order_id} | DELETE | 删除订单（需密码验证） |
| 订单 | /api/orders/{order_id}/qr-code | GET | 获取二维码 |
| 图片 | /api/images/upload-temp | POST | 上传临时图片（创建订单前） |
| 图片 | /api/images/upload-direct/{order_id} | POST | 直接上传图片（已有订单） |
| 图片 | /api/images/migrate/{temp_id}/{order_id} | POST | 迁移临时图片到正式目录 |
| 图片 | /api/images/list/{order_id} | GET | 获取订单图片列表 |
| 图片 | /api/images/delete/{img_id} | DELETE | 删除单张图片 |
| 图片 | /api/images/set-main/{img_id} | POST | 设置为主图 |
| 图片 | /api/images/clean-temp | POST | 清理48小时过期临时图片 |
| 日志 | /api/logs/operations | GET | 操作日志 |
| 统计 | /api/statistics/overview | GET | 综合统计概览（默认近365天） |
| 统计 | /api/statistics/sales/total | GET | 销售总金额统计（默认近365天） |
| 统计 | /api/statistics/commission/theoretical | GET | 理论应得提成（按销售时间，默认近365天） |
| 统计 | /api/statistics/commission/actual | GET | 实际应得提成（按发货时间，默认近365天） |
| 统计 | /api/statistics/commission/by-user | GET | 按用户统计提成（老板端，默认近365天） |
| 统计 | /api/statistics/factory-dashboard | GET | 工厂端工作台聚合（已发货/未生产/生产中/生产完成，仅boss、factory） |
| 统计 | /api/statistics/avg-shipping-time | GET | 平均发货时长统计（默认近365天） |
| 提成结算 | /api/commission-settlement/unpaid | GET | 获取未结算提成数据 |
| 提成结算 | /api/commission-settlement/unpaid/orders | GET | 获取未结算提成订单列表 |
| 提成结算 | /api/commission-settlement/pay | POST | 执行提成结算 |
| 账户提现 | /api/withdraw/shops | GET | 获取提现网店列表（按创建者分组） |
| 账户提现 | /api/withdraw/records | GET | 获取提现记录列表 |
| 账户提现 | /api/withdraw/records | POST | 新增提现记录 |
| 账户提现 | /api/withdraw/records/{id} | PUT | 更新提现记录 |
| 账户提现 | /api/withdraw/records/{id} | DELETE | 删除提现记录 |
| 账户提现 | /api/withdraw/records/export | GET | 导出提现记录Excel |
| 智慧大屏 | /api/dashboard/* | GET | 见下方「智慧大屏」接口组（旧 /api/statistics/dashboard/* 路径已下线，统一迁至 /api/dashboard/*） |
| 日志 | /api/logs/operations | GET | 操作日志（仅boss） |
| 日志 | /api/logs/login | GET | 登录日志（仅boss） |
| 日志 | /api/logs/cleanup/config | GET | 获取日志清理配置（仅boss） |
| 日志 | /api/logs/cleanup/config | PUT | 更新日志清理配置（仅boss） |
| 日志 | /api/logs/cleanup/preview | POST | 预览日志清理效果（仅boss） |
| 日志 | /api/logs/cleanup/execute | POST | 执行日志清理（仅boss） |
| 日志 | /api/logs/cleanup/records | GET | 获取日志清理记录（仅boss） |
| 通知 | /api/notifications | GET | 获取站内信列表 |
| 通知 | /api/notifications/unread-count | GET | 获取未读消息数量 |
| 通知 | /api/notifications/{id}/read | PUT | 标记单条消息为已读 |
| 通知 | /api/notifications/all/read | PUT | 标记所有消息为已读 |
| 通知 | /api/notifications/{id} | DELETE | 删除单条消息 |
| 数据清理 | /api/logs/cleanup/notification/config | GET | 获取站内信清理配置（仅boss） |
| 数据清理 | /api/logs/cleanup/notification/config | PUT | 更新站内信清理配置（仅boss，最小360天） |
| 数据清理 | /api/logs/cleanup/notification/preview | POST | 预览站内信清理效果（仅boss） |
| 数据清理 | /api/logs/cleanup/notification/execute | POST | 执行站内信清理（仅boss，需 confirm=true） |
| 智慧大屏 | /api/dashboard/overview | GET | 总览：总订单数、销售总额、待发货、生产中、已发货（仅boss） |
| 智慧大屏 | /api/dashboard/monthly-sales | GET | 近24个月月度销售趋势（参数 months，仅boss） |
| 智慧大屏 | /api/dashboard/shop-ranking | GET | 网店销售额TOP10（仅boss） |
| 智慧大屏 | /api/dashboard/overdue-orders | GET | 超期订单明细（待发货/虚拟发货 + 下单时长，仅boss） |
| 智慧大屏 | /api/dashboard/product-ranking | GET | 畅销商品TOP10（仅boss） |
| 智慧大屏 | /api/dashboard/sales-ranking | GET | 销售员销售额排行（仅boss） |
| 智慧大屏 | /api/dashboard/finance-summary | GET | 财务汇总（仅boss） |
| 智慧大屏 | /api/dashboard/sales-performance | GET | 销售业绩详情（仅boss） |
| 智慧大屏 | /api/dashboard/test | GET | 接口连通性测试 |
| 汇率 | https://api.exchangerate-api.com/v4/latest/CNY | GET | 获取汇率数据（外部API） |
| 健康检查 | /health | GET | 服务健康检查 |

## 7 数据统计功能说明

### 7.1 统计数据范围

所有统计接口默认查询近365天的数据，可通过`start_date`和`end_date`参数自定义时间范围。

> 例外：智慧大屏（`/api/dashboard/*`）默认统计近 **24 个月** 数据，用于呈现更长期的销售趋势与全球业务分布；其 `overview` 接口在原有总订单数/已发货/待发货基础上，新增返回「销售总额（sales_total）」与「生产中订单数（producing_orders）」。

> **生产状态统计口径（2026-08-10 起）**：已退货/退款（`refunded`）订单不计入「未生产 / 生产中 / 生产完成」统计——工作台 `process-flow`、工厂看板 `factory-dashboard`、智慧大屏 `overview` 及订单列表 `produce_status` 筛选（`orders.py`）均已排除 `shipping_status=refunded`，保证"卡片数字 = 点击跳转列表数量"一致。

### 7.2 角色差异化统计

| 角色 | 统计内容 |
| ----- | -------- |
| 老板端(boss) | 订单总金额、总订单数、已发货订单数、待发货订单数、平均发货时长、销售提成统计表格、订单生产状态分布 |
| 销售端(sales) | 我的销售总金额、我的订单数、理论应得提成、实际应得提成、生产状态简易统计 |
| 工厂端(factory) | 总订单数、待处理订单数、已完成订单数、未生产订单数、生产中订单数、已完成生产订单数 |
| 发货端(shipping) | 总订单数、待处理订单数、已完成订单数、生产状态简易统计 |

### 7.3 老板端销售提成统计表格

**功能说明**：按发货时间统计每个销售端用户的提成数据。

**表格字段**

| 字段 | 说明 | 格式 |
| ---- | ---- | ---- |
| 登录账号 | 销售用户的登录用户名 | 字符串 |
| 真实姓名 | 销售用户的真实姓名 | 字符串 |
| 提成比例 | 该用户的提成比例 | 百分比格式（如5%） |
| 销售金额 | 该用户的销售总金额 | ¥xxx,xxx.xx |
| 发货订单数 | 已发货的订单数量 | 数字 |
| 应得提成 | 该用户应得的提成金额 | ¥xxx,xxx.xx（高亮显示） |
| 单笔提成 | 平均每单提成金额 | ¥xxx.xx |

**统计汇总**

表格头部显示汇总信息：
- 总计提成：所有销售用户的提成总和
- 总计订单：所有销售用户的发货订单总数
- 总计销售：所有销售用户的销售总金额

**筛选功能**
- 日期范围筛选：默认显示当前月份数据（从本月1日到今天）
- 用户筛选：可选择查看全部用户或单个用户

### 7.4 平均发货时长统计

**计算公式**：`平均发货时长 = 平均（发货时间 - 订单创建时间）`

**显示格式**：以天为单位，保留2位小数；同时显示小时数（如：2.5天 = 60小时）

## 8 业务流程图

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           订单管理业务流程（含生产状态）                         │
└─────────────────────────────────────────────────────────────────────────────────┘

[销售端]                              [工厂端]                           [发货端]
    │                                     │                                  │
    ▼                                     │                                  │
创建订单 ────────────────────────────────>│                                  │
    │                                     │                                  │
    │ 生成追溯码+二维码                    │                                  │
    │                                     │                                  │
    │ produce_status = unproduce          │                                  │
    │                                     │                                  │
    │ 上传产品图片 ───────────────────────>│                                  │
    │                                     │                                  │
    ▼                                     ▼                                  ▼
订单列表（待发货）◄────────────────────────────────────────────────── 订单列表
    │                                     │                                  │
    │ （删除订单）                         │                                  │
    │ （输入密码验证）                     │                                  │
    │                                     │ 查看订单                          │
    │                                     │ 打印二维码                        │
    │                                     │                                  │
    │                                     │ 上传生产图片                      │
    │                                     │   │                              │
    │                                     │   ▼                              │
    │                                     │ produce_status = producing       │
    │                                     │   │                              │
    │                                     │   │ 自动推送通知                   │
    │                                     │   ▼                              │
    │                                     │ 继续生产...                      │
    │                                     │                                  │
    ▼                                     │                                  ▼
修改发货状态 ─────────────────────────────────────────────────> 填写物流信息
(pending → shipped/virtual)               │                           上传发货凭证
    │                                     │                                  │
    │ 自动触发:                           │                                  │
    │ produce_status = produced           │                                  │
    │ 永久锁定，不可回退                   │                                  │
    │                                     │                                  ▼
                                     订单完成 ◄─────────────────────────────┘
                                        (生产流程结束)
```

### 8.1 生产状态流转图

```
┌──────────────────┐     工厂上传生产图片      ┌──────────────────┐     订单发货      ┌──────────────────┐
│   未生产         │ ───────────────────────> │   生产中         │ ───────────────> │   生产完成       │
│  (unproduce)    │                          │  (producing)     │                  │  (produced)     │
│                  │                          │                  │                  │                  │
│  新建订单默认     │                          │  可手动修改       │                  │  永久锁定        │
│  可手动修改       │ <── 可手动修改 ───────── │  可手动修改       │                  │  不可下调        │
└──────────────────┘                          └──────────────────┘                  └──────────────────┘
                                                                                          │
                                                          撤销发货（shipping_status→pending）
                                                          生产状态维持produced不变，不可回退
```

## 9 系统配置参数

| 参数 | 默认值 | 说明 |
| ---- | ------ | ---- |
| SECRET_KEY | your-secret-key-change-in-production | JWT密钥 |
| ALGORITHM | HS256 | JWT算法 |
| ACCESS_TOKEN_EXPIRE_MINUTES | 1440 | Token有效期（24小时） |
| TEMP_IMAGE_RETENTION_HOURS | 24 | 临时图片保留时间 |
| QR_CODE_SIZE | 300 | 二维码尺寸 |
| DATABASE_URL | sqlite+aiosqlite:/// | SQLite数据库URL |
| STATISTICS_DEFAULT_DAYS | 365 | 统计数据默认查询天数 |

## 10 图片目录结构

```
data/images/
├── temp/                          # 临时图片目录
│   └── {uuid}.{ext}              # 临时图片文件
└── official/                     # 正式图片目录
    └── {order_id}/               # 按订单追溯码分组
        ├── sales/                # 销售端图片（商品图片）
        │   └── {uuid}.jpg
        ├── factory/              # 工厂端图片（生产进度）
        │   └── {uuid}.jpg
        └── shipping/             # 发货端图片（发货凭证）
            └── {uuid}.jpg
```

### 10.1 图片层级说明

| 层级 | 标识 | 用途 | 上传角色 |
| ----- | ----- | ---- | -------- |
| 销售端 | sales | 商品图片、产品展示 | sales |
| 工厂端 | factory | 生产进度、加工过程 | factory |
| 发货端 | shipping | 发货凭证、物流单据 | shipping |

## 11 日志记录规范

### 11.1 操作日志记录范围

- 用户登录/登出
- 订单新增、编辑、删除、状态变更
- 图片上传、删除
- 网店新增、编辑、删除、状态变更
- 物流公司新增、编辑、删除
- 用户管理（新增、编辑、删除、密码重置）

### 11.2 日志字段说明

| 字段 | 说明 |
| ---- | ---- |
| user_id | 操作人ID |
| username | 操作人用户名 |
| operation_type | 操作类型（add/edit/delete/update等） |
| operation_content | 操作内容详情，含修改前后值 |
| ip_address | 操作者IP地址 |
| created_at | 操作时间 |

## 12 数据清理管理

> 由原「日志清理管理」扩展升级而来，现统一纳管**日志清理**与**站内信清理**两类数据，入口整合在「系统设置」页面的标签页面板中。

### 12.1 功能概述

数据清理模块确保仅保留必要期限的运营数据，同时提供灵活的配置、预览、手动执行与定时自动执行能力。分为两个独立分区：

- **日志清理**：清理操作日志与登录日志（基于 `created_at` 时间戳）
- **站内信清理**：清理站内通知消息（基于 `created_at` 时间戳），默认最少保留 360 天

### 12.2 核心特性

1. **基于时间戳的筛选**：自动识别并删除超过保留期限的历史数据
2. **可配置的保留期限**：
   - 日志保留天数：默认 730 天（2 年），最小 30 天，最大 3650 天
   - 站内信保留天数：默认 360 天，最小 360 天（硬性约束，不可低于此值），最大 3650 天
3. **删除前确认机制**：手动清理前需二次确认，防止误操作
4. **操作记录**：所有清理操作记录在 `log_cleanup_records` 表，可追溯（`cleanup_type` 新增 `notification` 类型）
5. **定时自动执行**：
   - 日志清理：每日凌晨 2:00
   - 站内信清理：每日凌晨 2:10（与日志清理解耦，避免同一时刻资源争用）
6. **手动触发接口**：提供手动触发清理的 API 接口
7. **失败重试机制**：清理失败时自动重试，最多3次
8. **异常告警**：清理失败时记录错误日志

### 12.3 数据模型

#### 12.3.1 日志清理记录表（log_cleanup_record）

| 字段名 | 类型 | 是否主键 | 是否必填 | 说明 |
| ----- | ----- | -------- | -------- | ---- |
| id | INTEGER | 是 | 是 | 自增主键 |
| cleanup_type | TEXT | 否 | 是 | 清理类型：operation/login/all/notification |
| retention_days | INTEGER | 否 | 是 | 保留天数 |
| deleted_count | INTEGER | 否 | 是 | 删除数量，默认0 |
| status | TEXT | 否 | 是 | 状态：pending/success/failed |
| error_message | TEXT | 否 | 否 | 错误信息 |
| start_time | TIMESTAMP | 否 | 是 | 开始时间 |
| end_time | TIMESTAMP | 否 | 否 | 结束时间 |
| triggered_by | TEXT | 否 | 否 | 触发方式：manual/scheduled |
| operator_username | TEXT | 否 | 否 | 操作人用户名 |

### 12.4 系统配置

| 参数 | 默认值 | 说明 |
| ---- | ------ | ---- |
| LOG_RETENTION_DAYS | 730 | 日志保留天数（2年） |
| NOTIFICATION_RETENTION_DAYS | 360 | 站内信保留天数（最小 360，不可低于此值） |
| SCHEDULED_CLEANUP_TIME | 02:00 | 日志定时清理时间（24小时制） |
| SCHEDULED_NOTIFICATION_CLEANUP_TIME | 02:10 | 站内信定时清理时间（24小时制） |
| MAX_RETRY_COUNT | 3 | 清理失败最大重试次数 |

### 12.5 API接口

| 接口路径 | 方法 | 说明 |
| ------- | ---- | ---- |
| /api/logs/cleanup/config | GET | 获取日志清理配置 |
| /api/logs/cleanup/config | PUT | 更新日志清理配置 |
| /api/logs/cleanup/preview | POST | 预览日志清理效果 |
| /api/logs/cleanup/execute | POST | 执行日志清理（需确认） |
| /api/logs/cleanup/records | GET | 获取清理记录列表 |
| /api/logs/cleanup/notification/config | GET | 获取站内信清理配置（retention_days 默认 360、最小 360） |
| /api/logs/cleanup/notification/config | PUT | 更新站内信清理配置（受最小 360 天约束） |
| /api/logs/cleanup/notification/preview | POST | 预览站内信清理效果（返回待删除数量） |
| /api/logs/cleanup/notification/execute | POST | 执行站内信清理（需 `confirm=true`） |

### 12.6 前端功能

数据清理管理功能已整合到「系统设置」页面，采用**标签页（Tabs）**分区，包含「日志清理」与「站内信清理」两个独立分页：

**通用（两分区一致）：**
1. **保留天数配置**：日志 30–3650 天；站内信 360–3650 天（`<el-input-number :min="360">` 硬约束）
2. **待删除统计**：实时显示当前截止日期与即将删除的数据条数
3. **预览 / 执行**：先预览再二次确认执行，执行后展示删除数量
4. **清理记录**：统一的历史记录表，`cleanup_type` 以不同标签区分 操作/登录/全部/站内信

**站内信清理分区额外说明：**
- 保留天数默认 360 天，且最小不可低于 360 天（业务要求最少存储 360 天）
- 执行清理时按 `created_at` 早于 `NOW() - retention_days` 删除 `notifications` 表记录（分批 1000 条/批）

### 12.7 定时任务

- **日志清理**
  - 任务名称：每日日志清理
  - 执行时间：每日凌晨 2:00
  - 触发方式：APScheduler CronTrigger（hour=2, minute=0）
- **站内信清理**
  - 任务名称：每日站内信清理
  - 执行时间：每日凌晨 2:10
  - 触发方式：APScheduler CronTrigger（hour=2, minute=10）
- **容错机制（两类一致）**：
  - 1 小时宽限期（错过触发时间 1 小时内仍可执行）
  - 最多 1 个并发实例
  - 替换已存在的任务

## 13 安全机制

1. **密码存储**：使用passlib库进行密码哈希，不存储明文
2. **Token认证**：JWT令牌，有效期24小时
3. **权限控制**：后端接口层鉴权，前端按钮级权限控制
4. **数据隔离**：除boss/factory外，严格按创建人隔离数据
5. **敏感数据过滤**：factory/shipping角色不可查看金额相关信息
6. **操作留痕**：所有核心操作记录日志，不可删除篡改
7. **SQL注入防护**：使用SQLAlchemy ORM进行参数化查询，防止SQL注入攻击
8. **订单删除验证**：删除订单时需要输入当前登录账号的密码，确保操作安全性

## 14 数据获取与性能优化

### 14.1 数据获取机制

- **实时数据获取**：所有前端页面通过API从数据库实时获取数据，确保显示内容与数据库保持一致
- **数据更新机制**：
  - 页面初始化时自动获取数据
  - 操作（创建、编辑、删除）后自动刷新数据
  - 相关数据变化时通过Pinia store自动同步

### 14.2 性能优化措施

- **分页查询**：所有列表接口支持分页参数，减少单次查询数据量
- **参数化查询**：使用SQLAlchemy ORM进行参数化查询，提高查询效率并防止SQL注入
- **缓存机制**：二维码等静态资源使用文件系统缓存，减少重复生成
- **异步查询**：后端使用SQLAlchemy异步查询，提高并发处理能力
- **按需加载**：前端根据用户角色和权限按需加载数据，减少不必要的请求

### 14.3 数据更新验证机制

- **前端验证**：使用Element Plus表单验证，确保数据格式正确
- **后端验证**：API接口进行业务逻辑验证，确保数据完整性
- **事务处理**：数据库操作使用事务，确保数据一致性
- **自动刷新**：操作完成后自动刷新数据，确保前端显示与数据库一致
- **错误处理**：完善的错误处理机制，确保系统稳定性

## 15 系统架构优化

- **前后端分离**：采用Vue3 + FastAPI前后端分离架构，提高开发效率和系统可维护性
- **模块化设计**：后端API按功能模块划分，前端组件化开发
- **状态管理**：使用Pinia进行状态管理，实现跨组件数据共享和自动更新
- **路由守卫**：前端路由守卫实现基于角色的访问控制
- **请求拦截器**：Axios请求拦截器处理认证令牌，确保API请求安全

### 15.1 前端路由配置

#### 15.1.1 路由列表

| 路径 | 名称 | 组件 | 标题 | 权限 |
| --- | --- | --- | --- | --- |
| /login | Login | Login.vue | 登录 | 无 |
| / | - | Layout.vue | - | 需要登录 |
| /dashboard | Dashboard | Dashboard.vue | 工作台 | 需要登录 |
| /orders | Orders | Orders.vue | 订单管理 | 需要登录 |
| /products | Products | Products.vue | 商品管理 | boss |
| /shops | Shops | Shops.vue | 网店管理 | boss, sales |
| /users | Users | Users.vue | 用户管理 | boss |
| /logistics | Logistics | Logistics.vue | 物流管理 | boss |
| /settings | Settings | Settings.vue | 系统设置 | boss |
| /logs | Logs | Logs.vue | 日志管理 | boss |
| /images | Images | Images.vue | 图片管理 | 需要登录 |
| /statistics | Statistics | Statistics.vue | 数据统计 | 需要登录 |
| /notifications | Notifications | Notifications.vue | 站内信 | 需要登录 |
| /smart-dashboard | SmartDashboard | SmartDashboard.vue | 智慧大屏 | boss |
| /salary-settlement | SalarySettlement | CommissionSettlement.vue | 工资结算 | boss |
| /account-withdrawal | AccountWithdrawal | AccountWithdrawal.vue | 账户提现 | boss, sales |
| /refund-orders | RefundOrders | RefundOrders.vue | 退款订单（数据统计→销售分析） | boss, factory |
| /freight-statistics | FreightStatistics | FreightStatistics.vue | 运费统计（财务模块→费用统计） | boss |

#### 15.1.2 路由守卫逻辑

```
1. 访问需要认证的路由且未登录 → 跳转到 /login
2. 已登录状态访问 /login → 跳转到首页 /
3. 访问有权限限制的路由且当前角色不在允许列表 → 跳转到 /dashboard
4. 其他情况 → 正常访问
```

#### 15.1.3 工作台动态路由

`/dashboard` 路径根据用户角色自动跳转到对应的工作台页面：
- **boss** → BossDashboard.vue
- **sales** → SalesDashboard.vue
- **factory** → FactoryDashboard.vue
- **shipping** → ShippingDashboard.vue

## 16 界面优化说明

### 16.1 用户信息显示

- **右上角显示真实姓名**：将用户信息从显示账号（username）改为显示真实姓名（real_name）
- **降级处理**：当用户没有设置真实姓名时，显示账号作为降级方案

### 16.2 选择框尺寸优化

- **订单状态选择框**：宽度150px，下拉选项高度40px，确保内容完整显示
- **用户选择框**：宽度180px，下拉选项高度40px
- **日期选择器**：宽度260px
- **优化内容**：
  - 足够的宽度确保长文本不被截断
  - 合适的选项高度提升点击体验
  - 与页面其他元素保持协调的视觉比例

### 16.3 订单列表优化

- **新增发货时间列**：显示订单发货时间，格式为YYYY-MM-DD
- **关键词搜索增强**：支持搜索平台订单号、商品名称、销售金额
  - 销售金额转换为字符串后进行模糊匹配
  - 可通过输入金额的部分数字进行搜索

### 16.4 订单打印功能

- **打印入口**：订单详情弹窗右上角「打印」按钮
- **打印格式**：A4 纵向纸张 (210mm × 297mm)
- **打印内容**：
  - 上部左侧：商品名称、平台、订单号、下单时间、创建人、收货地址、备注
  - 上部右侧：订单二维码 (45mm方框)
  - 下部：商品大图 (200mm高区域)，优先显示销售图片层第一张
- **打印方式**：打开新窗口展示打印预览，点击页内「点击打印单据」按钮调用浏览器原生打印API，连接系统打印机
- **打印CSS**：自动隐藏打印按钮，禁止内容分页拆分

### 16.5 订单创建功能增强

- **新增下单时间选择**：在订单创建表单中添加日期选择器组件
- **时间格式**：精确到年月日，格式为YYYY-MM-DD
- **默认值**：默认选择当前系统日期
- **未来日期禁止**：禁用未来日期的选择，确保数据合理性
- **时间持久化**：用户选择的下单时间会保存到数据库的created_at字段
- **界面显示**：订单列表和详情页都会正确显示用户选择的下单时间

### 16.6 模块整合

#### 16.6.1 基础信息模块

将「商品管理」、「网店信息」、「用户管理」、「物流管理」合并为「基础信息」子菜单：

| 子菜单 | 路由 | 权限 |
| --- | --- | --- |
| 商品管理 | /products | boss |
| 网店信息 | /shops | boss, sales |
| 用户管理 | /users | boss |
| 物流管理 | /logistics | boss |

#### 16.6.2 财务模块

将「工资结算」和「账户提现」合并为「财务模块」子菜单：

| 子菜单 | 路由 | 权限 |
| --- | --- | --- |
| 工资结算 | /salary-settlement | boss |
| 账户提现 | /account-withdrawal | boss, sales |

#### 16.6.3 侧边栏菜单结构

```
智慧大屏          /smart-dashboard    (仅boss)
工作台            /dashboard
订单管理          /orders             (非factory)
基础信息 ▼        (子菜单)
├── 商品管理      /products           (仅boss)
├── 网店信息      /shops
├── 用户管理      /users              (仅boss)
└── 物流管理      /logistics          (仅boss)
数据统计          /statistics
财务模块 ▼        (子菜单)
├── 工资结算      /salary-settlement  (仅boss)
└── 账户提现      /account-withdrawal
系统设置          /settings           (仅boss)
日志管理          /logs               (仅boss)
─────────────────
汇率计算器        (底部固定区域)
```

#### 16.6.4 头部功能

- **站内信图标**：右上角显示，带未读消息数量角标
- **用户信息**：显示真实姓名+角色标签
- **下拉菜单**：修改密码、退出登录

### 16.7 汇率计算器

- **位置**：融入侧边栏底部区域，作为侧边栏的一部分，不使用悬浮卡片显示
- **布局**：侧边栏采用flex垂直布局，导航菜单占据剩余空间，汇率计算器固定在底部
- **默认状态**：显示 CNY 到 USD 的汇率信息，包含刷新按钮
- **展开功能**：点击卡片区域展开完整的汇率计算器
- **核心功能**：
  - 支持多种货币选择（CNY、USD、EUR、GBP、JPY、HKD、AUD、CAD）
  - 货币交换功能，一键切换源货币和目标货币
  - 实时汇率获取，使用免费的 ExchangeRate-API
  - 自动计算转换结果
  - 显示汇率更新时间
- **样式设计**：
  - 透明背景（rgba(255, 255, 255, 0.05)），细线边框
  - 控件使用small尺寸，适配侧边栏宽度
  - 与侧边栏深色主题融合，不突兀

### 16.8 老板端工作台卡片集成工厂端指标

老板端工作台（`BossDashboard.vue`）在原有「经营概览」基础上，新增「生产/发货概览」分区，直接复用工厂端聚合接口（`/api/statistics/factory-dashboard`），无需切换账号即可掌握生产进度。

**分区与卡片**

| 分区 | 卡片 | 点击行为 |
| --- | ---- | ---- |
| 经营概览 | 总订单数 | 跳转订单管理（全部） |
| 经营概览 | 待发货 | 跳转订单管理（筛选待发货） |
| 经营概览 | 总销售额 | 跳转数据统计页 |
| 经营概览 | 总提成 | 跳转工资结算页 |
| 生产/发货概览 | 已发货 | 跳转订单管理（筛选已发货） |
| 生产/发货概览 | 未生产 | 跳转订单管理（筛选未生产） |
| 生产/发货概览 | 生产中 | 跳转订单管理（筛选生产中） |
| 生产/发货概览 | 生产完成 | 跳转订单管理（筛选生产完成） |

**实现要点**
- 卡片支持点击联动：点击后携带查询参数（如 `?shipping_status=pending`、`?produce_status=unproduce`）跳转到订单管理页，`UniversalOrderList.vue` 会读取路由参数自动应用筛选条件。
- 卡片带 hover 上浮 + 蓝色边框的点击反馈；工厂端分区的 4 张卡片采用更紧凑的样式。
- 原「总销售额」「总提成」卡片此前恒为 ¥0，现已分别取自 `/api/statistics/overview` 与 `/api/statistics/commission/by-user` 汇总，正常显示。

## 17 智慧大屏系统

### 17.1 系统概述

智慧大屏数据可视化系统是面向企业管理层的专业级决策支持平台，采用现代化UI设计风格，实现数据实时展示、多维度分析和交互式操作功能。

### 17.2 核心功能模块（当前实现）

智慧大屏采用「顶部标题栏 + 左/中/右三栏」的沉浸式布局，数据全部来自 `/api/dashboard/*` 接口组，仅统计近 24 个月数据。

#### 17.2.1 顶部标题栏
- 主标题「全球业务数据智慧大屏」+ 副标签「此面板仅统计最近 24 个月内数据」
- 右侧实时时钟（每秒刷新）+ 手动「刷新」按钮（重新拉取全部接口）

#### 17.2.2 左侧面板
| 区块 | 内容 | 数据接口 |
| --- | --- | --- |
| 指标卡片 ×5 | 总订单数、销售总金额、待发货、生产中、已发货 | `/api/dashboard/overview` |
| 月度销售折线图 | 近 24 个月销售趋势（金额或订单数） | `/api/dashboard/monthly-sales` |
| 畅销品 TOP10 | 商品销量排行（横向条形） | `/api/dashboard/product-ranking` |

#### 17.2.3 中央面板（世界地图）
- ECharts 世界地图，15 个全球城市固定坐标以 `effectScatter` 涟漪散点展示
- 从国内 4 个枢纽城市（北京/上海/广州/深圳）向全球城市绘制动态 `lines` 飞线
- 鼠标悬停显示城市名称与数值，支持缩放/平移

#### 17.2.4 右侧面板
| 区块 | 内容 | 数据接口 |
| --- | --- | --- |
| 超期订单（上） | 待发货/虚拟发货且下单时长超阈值的订单明细，卡片式排行：左侧排名色条+订单ID（可点击弹详情）、右侧**发货状态 + 生产进度上下结构标签**、副行「下单时长」天数红色加粗；标题带「X单」计数 | `/api/dashboard/overdue-orders` |
| 网店销售 TOP30（下） | 各网店销售额**卡片式排行**（无表头）：左侧排名色条+店主名，右侧青色金额，下方灰色「网店ID」副标题 | `/api/dashboard/shop-ranking` |

> 2026-08-10 起：右侧面板上下两块已对调（超期订单在上、网店排行在下）；所有列表型模块（统计卡/月度趋势/畅销品/网店排行/超期订单）均改为**固定高度 + 内容超出内部滚动**，保证每块稳定显示不被挤压。

### 17.3 设计与技术要求

#### 17.3.1 视觉设计

- **主题**：深色主题为主色调
- **色彩方案**：高对比度、专业数据可视化色彩
- **响应式**：支持1920×1080到4K及以上分辨率
- **动画效果**：平滑过渡，避免界面闪烁
- **突出显示**：关键数据指标采用大字号、对比色、光晕效果

#### 17.3.2 性能要求

| 指标 | 要求 |
| ---- | ---- |
| 数据刷新频率 | 不低于5分钟/次 |
| 预警数据更新 | 实时 |
| 界面响应延迟 | ≤300ms |
| 图表渲染 | 大数据量流畅交互 |

#### 17.3.3 错误处理

- 网络异常友好提示
- 数据加载失败自动重试
- 完善的错误日志记录

### 17.4 组件结构

```
frontend/
├── public/
│   └── map/
│       └── world.json              # 世界地图 GeoJSON（ECharts 注册用）
└── src/
    ├── views/
    │   └── SmartDashboard.vue      # 智慧大屏主界面（单文件内联，含三栏布局与全部图表）
    └── api/
        └── dashboard.js            # 智慧大屏 API 封装
```

### 17.5 权限控制

- **访问权限**：仅boss角色可访问
- **路由配置**：`/smart-dashboard`
- **数据隔离**：敏感数据访问受限

### 17.6 界面布局

```
┌──────────────────────────────────────────────────────────────────────────┐
│ 全球业务数据智慧大屏          此面板仅统计最近24个月内数据    [时间] [刷新] │
├───────────────────────┬───────────────────────────────┬──────────────────┤
│ 左侧面板              │ 中央面板（世界地图）          │ 右侧面板        │
│ ┌─────────────────┐  │                               │ 网店销售 TOP10  │
│ │ 指标卡×5        │  │   ● 北京  ● 上海  ● 纽约      │ ┌──────────────┐ │
│ │ 总订单/销售额/  │  │      ╱╲                        │ │ ①店主 ¥金额  │ │
│ │ 待发货/生产中/  │  │   ╱    ╲  ● 伦敦   ● 东京     │ │  网店ID      │ │
│ │ 已发货          │  │  ● 广州 ────▶ ● 巴黎          │ │ ②店主 ¥金额  │ │
│ └─────────────────┘  │  （15城涟漪散点+枢纽飞线）     │ └──────────────┘ │
│ ┌─────────────────┐  │                               │ 超期订单        │
│ │ 月度销售折线图  │  │                               │ ┌──────────────┐ │
│ └─────────────────┘  │                               │ │ 单号(可点击) │ │
│ ┌─────────────────┐  │                               │ │ 天数         │ │
│ │ 畅销品 TOP10    │  │                               │ └──────────────┘ │
│ └─────────────────┘  │                               │                  │
└───────────────────────┴───────────────────────────────┴──────────────────┘
```

### 17.7 数据模型

#### /api/dashboard/overview 返回
| 字段 | 类型 | 说明 |
| ---- | ---- | ---- |
| total_orders | Number | 总订单数 |
| sales_total | Number | 销售总额（本期新增） |
| pending_orders | Number | 待发货订单数 |
| producing_orders | Number | 生产中订单数（本期新增） |
| shipped_orders | Number | 已发货订单数 |

#### /api/dashboard/monthly-sales 返回
| 字段 | 类型 | 说明 |
| ---- | ---- | ---- |
| months | Array<String> | 月份标签数组（如 2024-08） |
| sales | Array<Number> | 各月销售金额 |
| orders | Array<Number> | 各月订单数 |

#### /api/dashboard/shop-ranking 返回
| 字段 | 类型 | 说明 |
| ---- | ---- | ---- |
| shop_name | String | 网店名称 |
| sales | Number | 销售额（降序 TOP10） |

#### /api/dashboard/overdue-orders 返回
| 字段 | 类型 | 说明 |
| ---- | ---- | ---- |
| order_id | String | 订单号 |
| shop_name | String | 网店名称 |
| days | Number | 下单至今天数 |
| overdue_text | String | 超期时长（如"187天"） |
| shipping_status | String | pending / virtual |
| shipping_status_text | String | 待发货 / 虚拟发货 |
| produce_status | String | unproduce / producing / produced |
| produce_status_text | String | 未生产 / 生产中 / 生产完成 |

#### /api/dashboard/product-ranking 返回
| 字段 | 类型 | 说明 |
| ---- | ---- | ---- |
| product_name | String | 商品名称 |
| sales | Number | 销量（降序 TOP10） |

### 17.8 交互说明

#### 刷新与实时性
- 右上角「刷新」按钮重新拉取全部 `/api/dashboard/*` 接口
- 顶部时钟每秒刷新，无需刷新页面即可看到当前时间

#### 图表交互
- 地图：鼠标悬停显示城市名称与数值，支持滚轮缩放与拖拽平移
- 折线图/条形图：鼠标悬停显示 tooltip 明细
- 暂无点击卡片下钻与 Excel/PNG 导出（后续版本规划）

#### 全屏
- 大屏进入时自动隐藏系统侧边栏与顶栏，沉浸式铺满视口（见第 17.8 节）

#### 订单详情与退出
- **超期订单点详情**：右侧「超期订单」列表中订单ID可点击，点击后弹出订单详情弹窗（复用 `OrderDetailDialog.vue`），展示订单基础信息、图片、二维码、发货状态与**生产进度**，与「订单列表-查看」完全一致
- **退出大屏**：右上角「退出」按钮点击后返回订单管理页面（`router.push('/orders')`）

### 17.9 技术实现要点

#### 响应式布局
- 使用CSS Grid和Flexbox实现自适应布局
- 支持不同屏幕比例切换（16:9/4:3/21:9）
- 媒体查询适配移动端设备

#### 动画效果
- CSS动画实现平滑过渡
- SVG图表动画增强视觉效果
- 数据更新时数字滚动动画

#### 状态管理
- 使用Vue响应式系统管理状态
- 组件间通过props传递数据
- 支持实时数据更新

### 17.10 启动方式

智慧大屏系统作为订单管理系统的一个模块，需先启动主系统：

1. 运行 `启动器.py` 或 `一键启动.bat`
2. 使用boss角色账号登录（如：1001/1001）
3. 点击侧边栏「智慧大屏」菜单进入

## 18 前端模块化开发规范

### 18.1 模块化架构概述

本项目采用模块化架构设计，按业务域拆分独立模块，实现业务解耦、便于维护的目标。

### 18.2 目录结构

```
frontend/src/
├── api/                          # API接口统一管理（14个业务接口文件）
│   ├── baseUrl.js               # 环境域名配置
│   ├── request.js               # Axios实例封装
│   ├── index.js                 # API统一导出入口（14个模块）
│   ├── auth.js                  # 认证接口（登录、注册、获取用户）
│   ├── user.js                  # 用户管理接口（CRUD）
│   ├── order.js                 # 订单管理接口（列表、详情、CRUD、二维码）
│   ├── shop.js                  # 网店管理接口
│   ├── logistics.js             # 物流公司管理接口
│   ├── image.js                 # 图片管理接口（上传、迁移、删除）
│   ├── product.js               # 商品管理接口（CRUD、批量删除）
│   ├── statistics.js            # 数据统计接口（概览、提成、发货时长）
│   ├── logs.js                  # 日志管理接口（操作日志、登录日志、清理）
│   ├── notification.js          # 通知管理接口（站内信）
│   ├── exchange.js              # 汇率接口（外部API）
│   ├── dashboard.js             # 智慧大屏接口（5个专用API）
│   ├── commission_settlement.js # 提成结算接口
│   └── withdraw.js              # 账户提现接口
├── modules/                      # 业务模块目录（10个模块）
│   ├── Common/                  # 公共模块
│   │   └── components/          # 公共组件（8个）
│   │       ├── ExchangeCalculator.vue   # 汇率计算器（融入侧边栏）
│   │       ├── OrderOverview.vue       # 订单概览统计卡片
│   │       ├── SalesPerformance.vue    # 销售人员业绩分析表格
│   │       ├── FinanceStatistics.vue   # 财务数据统计图表
│   │       ├── SalesRanking.vue        # 销售排行榜（TOP10）
│   │       ├── ProductRanking.vue      # 商品热销排行榜（TOP20）
│   │       ├── SystemControls.vue      # 系统控制面板（全屏、时间选择）
│   │       └── world.json             # 世界地图数据（用于地址解析）
│   ├── Order/                   # 订单管理模块
│   │   ├── views/
│   │   │   └── Orders.vue              # 订单管理页面
│   │   ├── components/
│   │   │   └── UniversalOrderList.vue  # 通用订单列表组件（多角色动态权限）
│   │   └── services/
│   │       └── orderService.js         # 订单业务逻辑（数据转换、计算）
│   ├── Product/                 # 商品管理模块
│   │   └── services/
│   │       └── productService.js       # 商品业务逻辑
│   ├── Shop/                    # 网店管理模块
│   │   └── services/
│   │       └── shopService.js          # 网店业务逻辑
│   ├── Logistics/               # 物流管理模块
│   │   └── services/
│   │       └── logisticsService.js     # 物流业务逻辑
│   ├── Image/                   # 图片管理模块
│   │   └── services/
│   │       └── imageService.js         # 图片业务逻辑
│   ├── Log/                     # 日志管理模块
│   │   └── services/
│   │       └── logService.js           # 日志业务逻辑
│   ├── Statistics/              # 数据统计模块
│   │   └── services/
│   │       └── statisticsService.js    # 统计业务逻辑
│   ├── Settings/                # 系统设置模块
│   │   └── services/
│   │       └── settingsService.js      # 设置业务逻辑
│   ├── User/                    # 用户管理模块
│   │   └── services/
│   │       └── userService.js          # 用户业务逻辑
│   └── Finance/                 # 财务模块
│       └── views/
│           ├── CommissionSettlement.vue  # 工资结算（提成结算）
│           └── AccountWithdrawal.vue     # 账户提现
├── utils/                        # 工具函数（5个）
│   ├── request.js               # Axios封装
│   ├── format.js                # 格式化函数（日期、金额）
│   ├── countryData.js           # 国家数据（地图定位）
│   ├── addressParser.js         # 地址解析器（提取国家）
│   └── exchangeRequest.js       # 汇率请求工具
├── styles/                      # 样式目录
│   └── dark-theme.scss          # 深色主题样式（智慧大屏）
├── store/                        # Pinia状态管理（2个）
│   ├── user.js                  # 用户状态管理（登录信息、权限）
│   └── users.js                 # 用户列表状态管理
├── router/                       # 路由配置
│   └── index.js                 # Vue Router配置（含权限守卫）
└── views/                        # 根页面（16个）
    ├── Login.vue                # 登录页面
    ├── Layout.vue               # 主布局（侧边栏+主内容区+汇率计算器）
    ├── Dashboard.vue            # 工作台（角色路由）
    ├── BossDashboard.vue        # 老板端工作台
    ├── SalesDashboard.vue       # 销售端工作台
    ├── FactoryDashboard.vue     # 工厂端工作台
    ├── ShippingDashboard.vue    # 发货端工作台
    ├── Users.vue                # 用户管理
    ├── Shops.vue                # 网店管理
    ├── Orders.vue               # 订单管理（模块入口）
    ├── Products.vue             # 商品管理
    ├── Images.vue               # 图片管理
    ├── Logistics.vue            # 物流管理
    ├── Statistics.vue           # 数据统计
    ├── Logs.vue                 # 日志管理
    ├── Settings.vue             # 系统设置
    ├── Notifications.vue        # 站内信
    └── SmartDashboard.vue       # 智慧大屏
```

### 18.3 API接口规范

#### 18.3.1 目录约束

1. 所有后端请求接口全部归集至`src/api/`目录
2. api目录固定分层：
   - `baseUrl.js`：统一管理开发/测试/生产环境域名
   - `request.js`：axios实例、请求/响应拦截器
   - 业务api文件：按业务拆分（auth、user、order等）
   - `index.js`：统一导出入口

#### 18.3.2 编写规则

1. 单一接口文件只对应一类业务
2. 接口函数仅做网络请求，禁止混入业务逻辑
3. 所有接口请求统一复用`request.js`导出的axios实例
4. 请求地址、请求方式、参数严格封装在接口函数内

#### 18.3.3 调用规则

1. 页面、组件统一从`@/api`导入接口
2. 鉴权Token、请求超时、统一报错提示全部由request拦截器处理
3. 接口参数传递规范：GET请求使用params传参，POST请求使用data传参

### 18.4 模块拆分规范

#### 18.4.1 模块划分标准

1. 按业务域拆分独立模块，禁止按单一功能零散拆分
2. 每个业务模块独立闭环，内部包含页面、私有组件、模块专属逻辑hooks
3. 模块分层约束：
   - 业务模块仅允许依赖`api`、`utils`、`store`公共目录
   - 业务模块之间禁止直接import组件/方法
4. 拆分粒度控制：单一业务域代码量低于200行不单独拆模块

#### 18.4.2 模块依赖禁止项

1. 禁止模块循环引用
2. 禁止A模块直接import B模块内部页面、私有函数
3. 禁止在模块组件内硬编码接口地址、环境域名、常量编码

### 18.5 业务逻辑层规范

#### 18.5.1 职责划分

- **API层**（`src/api/`）：仅负责网络请求，不包含业务逻辑
- **Service层**（`src/modules/*/services/`）：负责业务逻辑、数据处理、计算等
- **View层**（`src/modules/*/views/`）：负责UI展示和用户交互

#### 18.5.2 Service层规范

1. 所有业务逻辑统一放在模块的service层
2. Service层函数负责数据计算、格式化、验证等业务逻辑
3. Service层统一从`@/api`导入接口，禁止直接调用axios
4. Service层函数必须包含错误处理和日志记录

### 18.6 维护约束

1. 修改接口域名、请求拦截逻辑，仅修改`api`目录下对应文件
2. 新增业务功能，优先在对应业务模块内新增代码
3. 接口新增、修改仅改动对应业务api文件
4. 删除废弃业务，直接删除完整业务模块文件夹
5. 全局通用工具统一放`utils`，模块禁止重复实现

### 18.7 禁止行为清单

1. 禁止在vue页面、组件、hooks中直接写axios请求代码
2. 禁止将所有接口全部堆入同一个api文件
3. 禁止业务模块之间互相引入内部组件、函数
4. 禁止硬编码后端地址、Token、错误提示文案
5. 禁止在API函数内编写业务计算、弹窗交互逻辑

## 19 启动器使用说明

### 19.1 启动方式

系统提供两种启动方式，推荐使用可视化启动器：

| 启动方式 | 文件 | 说明 |
| -------- | ---- | ---- |
| **推荐** | `启动器.py` | 可视化交互界面，实时日志显示 |
| 备用 | `一键启动.bat` | 批处理脚本，自动启动前后端服务 |

### 19.2 可视化启动器功能

> 启动器已由 **Tkinter 重写为 PySide6（Qt6）**（2026-08-08），窗口 880×620 无边框浅色风格，最大化走 Qt 原生 `showMaximized`，根除旧版合成器双窗口堆叠 bug。

#### 界面特性（PySide6 浅色风格）
- **左侧 170px 侧边栏**：顶部品牌 logo +「牛蛙产销协同」标题与版本号；中部导航项「状态」（服务概览）、「备份」（数据库备份与还原）、「初始」（恢复出厂，需输入特殊操作密码），点击切换主窗口视图。
- **服务概览卡片**：两个服务磁贴（「后端接口服务」绿、「前端网页客户端」蓝），磁贴含图标、SRV 标签、服务名、状态文字；每个磁贴下方一张「服务状态」卡片——波浪进度条（WaveBar）+ 状态标签，动画随状态切换：
  - 已停止：白色轨道无动画，标签「已停止」
  - 运行中：渐变绿 + 3 条白色波浪缓慢流动，标签「运行中」
  - 异常告警：橙红轨道 + 高频闪烁波纹，标签「异常告警」
  - 后端状态判定：端口检测 + `/health` HTTP 探测（`is_service_healthy()`），避免「端口占用但进程崩溃」误报运行中。
- **实时日志卡片**：标签页 4 个——综合 / 前端 / 后端 / 备份，彩色显示；顶部含检索框与「搜索」按钮（按关键字过滤当前标签页）。
- **底部 4 个功能按钮**：启动系统 / 停止系统 / 打开页面 / 刷新状态（文字统一黑色，启动中高亮、禁用态变暗）。
- **单实例保护**：socket 端口锁（25999）防止重复启动产生多窗口；支持最小化到系统托盘。
- **看门狗**：启动器进程存活期间，若后端/前端意外退出会自动重启；「停止系统」先停自管子进程，再按端口兜底清理。

#### 启动流程
1. 双击运行 `启动器.py`
2. 点击「启动系统」按钮
3. 等待环境检查和依赖安装（首次运行可能需要几分钟）
4. 服务启动完成后自动打开浏览器

#### 使用注意事项
- 服务运行期间不要关闭启动器窗口
- 关闭窗口时会提示确认（防止误关闭）
- 服务在后台静默运行，不会弹出命令行窗口

### 19.3 启动器界面预览

```
┌──┬─────────────────────────────────────────────────────────┐
│✓ │  牛蛙产销协同                                            │
│  │                                                         │
│状态│  ┌── 服务概览 ───────┐   ┌── 实时日志 ──────────────┐ │
│备份│  │ [后端接口服务]     │   │ 综合 │ 前端 │ 后端 │ 备份 │ │
│初始│  │  运行中           │   │ [检索日志__________] [搜索]│ │
│  │  │  ┌─服务状态─┐      │   │ [14:40:00][系统] ✅ Python…│ │
│  │  │  │▮▮▮波浪 运行中│  │   │ [14:40:03][后端] INFO: … │ │
│  │  │  └──────────┘      │   └───────────────────────────┘ │
│  │  └────────────────────┘                                 │
│  │                                                         │
│  │  [ 启动系统 ]  [ 停止系统 ]  [ 打开页面 ]  [ 刷新状态 ]  │
└──┴─────────────────────────────────────────────────────────┘

备份视图（点击侧边栏「备份」后切换，不弹窗）：
  ┌── 数据库备份与还原 ──────────────────────────────┐
  │ ① 备份设置  ② 自动备份配置  ③ 还原操作  ④ 操作日志 │
  └──────────────────────────────────────────────────┘
```

### 19.4 数据库备份与还原（主窗口内备份视图）

启动器侧边栏「备份」导航项，点击后**在主窗口内切换到「数据库备份与还原」视图**（替换首页内容，不弹出独立窗口）；点击「状态」返回首页。该视图集中管理数据库备份与还原：

**功能分区**：
- **备份设置**：显示当前备份目录，支持「选择目录」与「立即备份」。备份采用 SQLite `VACUUM INTO` 在线热备，不中断后端服务；备份文件命名 `order_system_backup_YYYYMMDD_HHMMSS.db`。
- **自动备份配置**：启用开关 + 周期模式 + 联动控件，配置持久化至 `backend/data/launcher_config.json`：
  - **每日**：显示执行时间选择器（HH:mm），每天固定时刻备份；
  - **每周**：显示星期下拉 + 执行时间，每周指定星期几备份；
  - **每月**：显示日期下拉（1–31）+ 执行时间，每月几号备份（日期超过当月最大天数自动取月末）；
  - **每隔几小时**：显示间隔小时数字输入框（限 1–24 整数，非法输入保存时弹窗拦截），循环间隔备份。
  - 定时任务在后台线程运行，仅启动器打开期间生效，退出自动停止，不注册系统计划任务；备份成功/失败均写入操作日志，视图显示下次备份时间。
- **还原操作**：列出已有备份文件（含大小、时间），支持刷新与「还原选中备份」。还原流程：先停止前后端服务 → 对当前库做 emergency 备份 → 用选中备份覆盖数据库 → 自动重启服务。
- **操作日志**：实时显示备份 / 还原的成功、失败与进度信息（同时分流写入首页「实时日志」的「备份」标签页）。

> 说明：后台 Web「系统设置」模块仅保留提成比例、临时图片保留时间等配置，**不再包含「数据备份路径」表单项**；数据库备份 / 还原的真实入口即本视图。另侧边栏「初始」导航为恢复出厂（停服务 → 跑 `数据库初始化.py --force` → 重启），需输入特殊操作密码。

## 20 商品管理功能说明

### 20.1 功能概述

商品管理模块用于管理系统中的商品信息，支持商品的增删改查操作，并与订单管理模块关联。

### 20.2 核心功能

| 功能 | 说明 | 权限要求 |
| ---- | ---- | -------- |
| 商品列表 | 展示商品列表，支持分页、搜索、筛选 | 所有角色 |
| 商品添加 | 创建新商品，包含字段验证 | boss、sales |
| 商品编辑 | 修改商品名称和备注 | boss、sales |
| 商品删除 | 删除单个商品（已被订单引用的商品不可删除） | boss |
| 批量删除 | 批量删除多个商品 | boss |
| 商品详情 | 查看商品完整信息 | 所有角色 |

### 20.3 数据模型

#### 商品表（product）

| 字段名 | 类型 | 是否必填 | 说明 |
| ------ | ---- | -------- | ---- |
| id | INTEGER | 是 | 自增主键 |
| product_code | TEXT | 是 | 商品唯一标识码（PRD-XXXXXXXX格式） |
| product_name | TEXT | 是 | 商品名称（2-100字符） |
| product_remark | TEXT | 否 | 商品备注（最大500字符） |
| status | TEXT | 是 | 商品状态：active/inactive |
| created_by | TEXT | 是 | 创建人用户名 |
| created_at | TIMESTAMP | 是 | 创建时间 |
| updated_at | TIMESTAMP | 是 | 更新时间 |

#### 商品编码生成规则
- **格式**：`PRD-{UUID前8位}`（采用UUID v4标准）
- **示例**：PRD-A1B2C3D4
- **唯一性**：系统自动检查，确保全局唯一

### 20.4 API接口列表

| 接口路径 | 方法 | 说明 | 权限 |
| -------- | ---- | ---- | ---- |
| /api/products/ | GET | 获取商品列表 | 所有角色 |
| /api/products/ | POST | 创建商品 | boss、sales |
| /api/products/{product_code} | GET | 获取商品详情 | 所有角色 |
| /api/products/{product_code} | PUT | 更新商品 | boss、sales |
| /api/products/{product_code} | DELETE | 删除商品 | boss |
| /api/products/batch-delete | POST | 批量删除商品 | boss |
| /api/products/count/total | GET | 获取商品统计 | 所有角色 |

### 20.5 前端页面功能

#### 商品列表页面
- 关键词搜索（商品编码、商品名称）
- 状态筛选（启用/停用）
- 分页展示
- 批量选择和批量删除
- 操作按钮：查看、编辑、删除

#### 商品创建/编辑表单
- 商品名称（必填，2-100字符）
- 商品备注（选填，最大500字符）
- 表单验证（前端实时验证 + 后端二次验证）

#### 订单关联
- 创建订单时，商品名称通过下拉框选择
- 下拉框仅显示已存在的商品，禁止输入不存在的商品名称
- 商品名称与订单表的product_name字段关联

### 20.6 权限控制

| 角色 | 权限 |
| ---- | ---- |
| boss | 完整权限（查看、创建、编辑、删除、批量删除） |
| sales | 查看、创建、编辑（不可删除） |
| factory | 仅查看 |
| shipping | 仅查看 |

### 20.7 删除限制

- 已被订单引用的商品不允许删除
- 删除前系统会检查订单表中是否存在引用
- 删除失败时显示具体原因（如：该商品已被订单 XXX 引用）

## 21 部署与维护

- **轻量化部署**：使用SQLite数据库，无需额外服务依赖
- **启动脚本**：提供可视化启动器和批处理脚本，简化部署流程
- **日志管理**：完善的日志记录机制，便于问题排查
- **数据备份**：定期备份数据库和图片文件，确保数据安全
- **性能监控**：通过日志分析系统性能，及时发现并解决问题

## 22 功能验证要点

### 22.1 用户信息显示验证

- 登录后右上角显示真实姓名（如「张三」）而不是账号（如「1001」）
- 当用户没有设置真实姓名时，显示账号

### 22.2 数据统计验证

- 数据统计页面默认显示近365天的数据
- 老板端显示「平均发货时长」统计卡片
- 老板端显示「销售提成统计表格」，默认显示当前月份数据
- 提成比例正确显示为百分比格式（如5%，而非500%）
- 可通过日期选择器筛选提成统计时间范围
- 可按用户筛选提成统计数据
- 汇总数据正确计算

### 22.3 订单管理验证

- 订单列表显示发货时间列
- 订单列表显示创建人列
- 可通过关键词搜索订单（支持平台订单号、商品名称、销售金额）
- 选择框内容完整显示，无截断
- 订单创建、编辑、删除功能正常
- 订单创建表单中显示"下单时间"日期选择器
- 默认选择当前日期
- 无法选择未来日期
- 用户选择的下单时间会保存到数据库
- 订单列表和详情页正确显示用户选择的下单时间
- 订单列表和详情页正确显示订单创建人的真实姓名
- 订单详情弹窗右上角展示「打印」按钮，点击可打开A4打印预览窗口
- 打印预览中正确显示订单信息、二维码、商品图片

### 22.4 基础信息模块验证

- 点击「基础信息」菜单进入合并后的页面
- 页面包含「网店管理」和「物流公司管理」两个标签页
- 可正常切换标签页
- 网店和物流公司的增删改查功能正常

### 22.5 汇率计算器验证

- 汇率计算器融入侧边栏底部区域，作为侧边栏的一部分显示
- 不使用悬浮卡片显示，与侧边栏深色主题融合
- 默认显示 CNY 到 USD 的汇率信息
- 点击卡片区域展开完整的汇率计算器
- 点击"刷新"按钮可更新汇率数据
- 支持多种货币选择（CNY、USD、EUR、GBP、JPY、HKD、AUD、CAD）
- 点击交换按钮可以一键切换源货币和目标货币
- 输入金额后自动计算转换结果
- 显示汇率更新时间
- 网络请求失败时显示友好的错误提示
- 加载时显示加载状态指示器
- 控件使用small尺寸，适配侧边栏宽度

### 22.6 数据清理管理验证

- 进入「系统设置」页面可以看到「数据清理管理」标签页面板（日志清理 / 站内信清理）
- 日志清理：可查看和设置保留天数（30-3650天），预览、二次确认执行，查看历史记录
- 站内信清理：默认保留 360 天、最小 360 天（输入框无法低于 360），可预览待删除数量并执行
- 切换分页互不干扰，清理记录表正确区分 cleanup_type（含「站内信」标签）
- 订单详情弹窗：可看到「生产进度」卡片，展示当前状态/更新时间/更新人，下拉修改后即时生效
- 智慧大屏：网店销售 TOP10 为卡片式（无表头），超期订单点击订单ID可弹出订单详情

### 22.7 商品管理验证

- 商品列表正常显示，支持搜索、筛选、分页
- 商品添加功能正常，表单验证生效
- 商品编辑功能正常，修改后数据正确保存
- 商品删除功能正常（boss角色）
- 商品详情页显示完整信息
- 订单创建时商品下拉框仅显示已存在的商品
- 权限控制正确（boss/sales可编辑，factory/shipping仅查看）
- 已被订单引用的商品无法删除

## 23 财务模块

### 23.1 模块概述

财务模块包含三个子模块：
- **工资结算**：按发货时间统计每月每个销售的提成金额，并支持标记已发放
- **账户提现**：网店提现记录管理功能
- **费用统计**（2026-08-10 起新增，由原"数据统计"模块迁入）：运费统计 `/freight-statistics`，按订单时间段统计运费，仅老板端可见

> 菜单结构：财务模块下分「财务结算（工资结算/账户提现）」与「费用统计（运费统计）」两个分组。

### 23.2 工资结算

#### 功能说明
- 按发货时间统计每个销售的提成金额
- 支持按月筛选统计数据
- 标记提成已发放后，订单中添加提成已发放标记，不再统计该订单

#### API接口

| 接口路径 | 方法 | 说明 |
| -------- | ---- | ---- |
| /api/commission-settlement/unpaid | GET | 获取未发放提成列表 |
| /api/commission-settlement/unpaid/orders | GET | 获取未发放提成订单详情 |
| /api/commission-settlement/pay | POST | 标记提成已发放 |

### 23.3 账户提现

#### 功能说明
- 网店提现记录管理
- 左侧网店列表按创建者分组显示
- 右侧显示对应网店的提现记录
- 支持新增、编辑、查询、导出功能
- 区分销售端/老板端权限

#### 数据模型

##### 网店提现记录表（shop_withdraw_record）

| 字段名 | 类型 | 是否主键 | 是否必填 | 说明 |
| ------ | ---- | -------- | -------- | ---- |
| id | INTEGER | 是 | 是 | 自增主键 |
| shop_id | TEXT | 否 | 是 | 网店ID |
| withdraw_date | DATE | 否 | 是 | 提现日期 |
| withdraw_amount | NUMERIC(12,2) | 否 | 是 | 提现金额 |
| remark | TEXT | 否 | 否 | 备注 |
| create_operator_name | TEXT | 否 | 是 | 创建人姓名 |
| create_operator_id | INTEGER | 否 | 是 | 创建人ID |
| update_operator_name | TEXT | 否 | 否 | 更新人姓名 |
| update_operator_id | INTEGER | 否 | 否 | 更新人ID |
| create_time | TIMESTAMP | 否 | 是 | 创建时间 |
| update_time | TIMESTAMP | 否 | 是 | 更新时间 |

#### API接口

| 接口路径 | 方法 | 说明 |
| -------- | ---- | ---- |
| /api/withdraw/shops | GET | 获取网店列表（按创建者分组） |
| /api/withdraw/records | GET | 获取提现记录列表 |
| /api/withdraw/records | POST | 新增提现记录 |
| /api/withdraw/records/{id} | PUT | 更新提现记录 |
| /api/withdraw/records/{id} | DELETE | 删除提现记录 |
| /api/withdraw/export | GET | 导出提现记录 |

#### 网店列表分组显示

网店列表按创建者（用户管理中的真实姓名）进行分组显示：

```
👤 张三 (3个网店)
  ├── SHOP001 - 淘宝旗舰店
  ├── SHOP002 - 京东自营店
  └── SHOP003 - 拼多多店

👤 李四 (2个网店)
  ├── SHOP004 - 抖音店
  └── SHOP005 - 快手店
```

点击网店ID后，右侧展示对应网店的提现记录管理功能。

## 24 启动器优化

### 24.1 功能改进

#### 服务状态检测
- 启动器打开后自动检测后端（8000端口）和前端（5173端口）服务是否已启动
- 根据检测结果更新状态显示：运行中/未启动
- 已启动的服务显示绿色指示灯，未启动显示灰色

#### 强制重启功能
- 即使服务已运行，点击「启动系统」仍可执行重启
- 自动检测并关闭占用端口的进程
- 等待2秒后启动新服务

#### 进度条优化
- 后端和前端进度条独立更新
- 启动过程中进度条平滑增长，使用圆角矩形绘制填充区域
- 运行中显示完整进度（100%），颜色变为绿色
- 使用标签（tags）管理进度条元素，避免引用失效

#### 日志标签页切换
- 四个标签页：综合、前端、后端、备份（备份日志由 `add_log(channel='backup')` 单独分流）
- 点击标签可切换显示对应日志内容
- 滚动条与当前日志文本同步
- 标签页切换使用pack/pack_forget方式控制显示

#### 按钮悬停效果
- 鼠标悬停时按钮颜色变亮
- 文字始终保持显示，不会被背景覆盖（使用btn_text标签管理文字）
- 停止按钮点击正常响应，事件绑定不会被覆盖

#### 修复内容
- 修复进度条显示异常：使用标签管理替代item_id引用，避免进度条元素被删除后无法更新
- 修复按钮文字消失：使用btn_text标签删除文字而非delete('all')，保留事件绑定
- 修复停止按钮无反应：移除delete('all')调用，确保事件绑定不被清除

### 24.2 界面样式

#### 磨砂深色设计（Frosted Dark）
- 深灰蓝主背景（#2c3138），卡片底色（#374151），磨砂玻璃质感圆角卡片（12px 圆角）
- 左侧 72px 磨砂深色侧边栏（#1e2227），蓝色 ✓ logo + 🏠 首页 / ⚙ 设置 两个有效入口
- 服务磁贴采用六边形图标 + 竖条均衡器波形 + 带白色圆形滑块的进度条 + 开关 toggle
- 底部功能按钮为左右两块式：左侧图标+文字、右侧纯色色块，高 120px

#### 颜色系统
- 成功绿（#4ade80 / #22c55e）：运行中、启动按钮、激活态
- 品牌蓝（#3b82f6 / #2584e8）：前端服务、停止/打开/刷新按钮色块
- 警示橙（#f59e0b）：异常状态
- 紫色（#a78bfa）：辅助强调
- 深灰终端背景（#1e2228）：日志区域

> 注：早于本版本的启动器曾采用浅色「苹果风格」UI，已在磨砂深色风格重设计中整体替换，旧配色（#f5f5f7 / #007aff / #34c759 / #ff3b30）不再使用。

## 25 近期修复与变更记录

> 以下记录本会话期间完成的主要修复与功能变更，便于后续维护回溯。

### 25.1 启动器与运行稳定性
- **健康检查增强**：`启动器.py` 新增 `is_service_healthy()`，对后端 `/health` 发送 HTTP 请求，端口占用但服务不可用不再误报「运行中」。
- **SQLite 写锁修复**：`get_orders` 原先在每次读取时写 `order_days` 字段，结合过大的连接池（pool_size=20）在 SQLite 单写者模型下造成全部连接阻塞、网页无响应；已移除读取时的写库操作，并将 `database.py` 连接池调整为 `pool_size=5 / max_overflow=10 / timeout=30`。

### 25.2 智慧大屏重构
- 由「理想化设计稿」落地为可运行的三栏布局：顶部标题栏+时间+刷新、左侧指标卡×5+月度折线图+畅销TOP10、中央 ECharts 世界地图（15 城涟漪散点 + 枢纽飞线）、右侧网店TOP10 + 超期订单表格。
- 新增后端接口 `/api/dashboard/monthly-sales`、`/api/dashboard/shop-ranking`、`/api/dashboard/overdue-orders`；`/api/dashboard/overview` 新增 `sales_total`、`producing_orders` 字段。
- 世界地图 GeoJSON 改为 `frontend/public/map/world.json` 静态资源 + `echarts.registerMap` 注册，不再逐单做地址解析；旧 `/api/statistics/dashboard/*` 路径已下线，统一为 `/api/dashboard/*`。

### 25.3 老板端工作台
- 新增「生产/发货概览」分区（已发货/未生产/生产中/生产完成），复用 `/api/statistics/factory-dashboard` 聚合接口。
- 全部 8 张卡片支持点击联动：携带 `shipping_status` / `produce_status` 查询参数跳转订单管理页，`UniversalOrderList.vue` 读取路由参数自动筛选。
- 修复「总销售额」「总提成」卡片恒为 ¥0 的问题（改取 overview 与 commission/by-user 汇总）。

### 25.4 订单管理
- 修复点击搜索后弹出「获取订单列表失败」但订单仍正常加载的问题：图片加载分支引用了未定义变量 `orderIds`，已改为 `orders.value`。
- 图片加载由循环串行请求改为 `Promise.all` 并行，消除 N+1 查询。

### 25.5 前序批量修复（共 28 项）
涵盖安全（密码改 Body 传参、CORS 白名单、路径遍历防护、注册角色校验）、逻辑（订单号生成器 RLock/deque、提成百分比整数比较、事务内 `flush` 替代 `commit`、状态日志取值、日志起止日期生效、SQLite 删除 `LIMIT` 改用子查询、CN 时区事件监听、删除校验双字段匹配、禁止修改 shop_id、`.model_dump()` 替代 `.dict()`）、前端（`validate` 异步回调模式、分页总数跟随筛选、跳转改用 `router.push`、移除多余 `console.log` 等）。

### 25.6 本批功能文档化（2026-07-16）

将以下已上线功能同步补充至本文档：

- **生产进度功能**：订单详情弹窗（`OrderDetailDialog.vue` 及 `Notifications.vue` 内联弹窗）右侧栏新增生产进度卡片，展示 `produce_status` 状态标签、更新时间/更新人，支持下拉修改（复用 `PUT /api/orders/{order_id}`）。详见第 4.5.8 节。
- **智慧大屏相关**：
  - 网店销售 TOP10 改为卡片式排行（无表头，排名色条+店主名+青色金额+网店ID副标题）
  - 超期订单列表订单ID可点击，弹出订单详情弹窗（含生产进度）
  - 右上角「退出」按钮返回订单管理页（`/orders`）
  - 详见第 17.2.4、17.6、17.8 节
- **站内信清理 + 日志清理重设计**：「日志清理管理」升级为「数据清理管理」标签页面板，新增站内信清理分区（默认保留 360 天、最小 360 天、每日 2:10 定时清理），新增 4 个站内信清理接口。详见第 12 章、第 6.2 节。

> 说明：智慧大屏呼吸点已按订单金额高低做**蓝→青→黄→红热力渐变**上色——金额越高的国家颜色越偏暖（红），越低越偏冷（蓝）；呼吸涟漪与光晕同步取该点颜色。金额归一化以各国最大订单金额为基准，颜色由 `heatColor(t)` 函数插值计算，tooltip 金额文字也按同一热力色显示。点的大小仍按金额缩放（14~42px）。


### 25.7 手机端作业页与启动器备份还原（2026-07-23）

- **手机端轻量作业页（`/m/`）**：新增独立于 PC 端的手机端作业页 `backend/mobile/index.html`（同源托管于 `/m/`，无需跨域），面向工厂 / 发货一线人员。功能：登录（复用 JWT）→ 扫码定位订单（jsQR 解析订单号，支持摄像头实时扫码与「从相册选二维码」兜底；HTTP 局域网下摄像头受限，相册选图可正常工作）→ 按角色查看 / 修改订单（工厂改生产状态 + 备注，发货改发货信息 + 备注）→ 上传图片（拍照或从相册选择，按角色写入 production / shipping 图层）。兼容现有订单二维码（编码订单号），无需重印。
- **启动器数据库备份 / 还原设置（主窗口内视图）**：启动器左侧「⚙ 设置」图标，点击后**在主窗口内切换到「数据库备份与还原」设置视图**（非独立窗口；点击 🏠 返回首页）。含备份目录选择、立即备份（`VACUUM INTO` 在线热备）、自动备份计划（每日 / 每周 / 每月，持久化 `backend/data/launcher_config.json`）、还原选中备份（先停服务 → emergency 备份 → 覆盖 → 重启）、操作日志。对应 `启动器.py`。
- **后台系统设置调整**：Web 端「系统设置」移除「数据备份路径」表单项（真实备份配置入口已移至启动器设置视图）。
- **配置位置调整**：`launcher_config.json` 由项目根目录迁移至 `backend/data/launcher_config.json`，并在 `.gitignore` 中忽略（含本机路径、会被自动备份调度改写）。
- **启动器 UI 整体重设计（磨砂深色风格）**：`启动器.py` 按设计稿重设计为磨砂深色 UI——左侧 72px 磨砂侧边栏（✓ logo + 🏠 首页 + ⚙ 设置）、主区「服务概览」+「实时日志」双卡片、服务磁贴（六边形图标 + 竖条均衡器波形 + 带白色圆形滑块的进度条 + 状态文字 + 开关 toggle）、实时日志扩展为综合 / 前端 / 后端 / 备份 4 个标签页并新增检索框与搜索按钮、底部 4 个左右两块式功能按钮（启动系统 / 停止系统 / 打开页面 / 刷新状态，高 120px）。所有基本功能逻辑（启动 / 停止 / 打开页面 / 刷新状态、备份还原、看门狗、自动备份调度）保持不变；侧边栏仅保留首页与设置两个有效入口，其余占位图标已移除；服务概览内后端 / 前端磁贴为上下堆叠结构。


### 25.8 2026-08-08 ~ 2026-08-10 变更记录

- **启动器 PySide6 重写**：`启动器.py` 由 Tkinter 彻底重写为 PySide6（880×620 无边框浅色），最大化走 Qt 原生 `showMaximized`，根除 Tkinter 合成器双窗口堆叠 bug；侧边栏三导航「状态 / 备份 / 初始」（初始=恢复出厂，需特殊操作密码）；服务磁贴简化为「图标+SRV标签+服务名+状态」，磁贴下方新增**服务状态卡片**（WaveBar 波浪进度条三态：已停止白色无动画 / 运行中渐变绿缓慢波浪 / 异常告警橙红高频闪烁）。详见第 19 章。
- **自动备份配置增强**：备份模式由「每日/每周/每月」扩展为 4 种，新增**「每隔几小时」**（1–24 整数，非法输入保存弹窗拦截）；表单联动：每日→时间选择器、每周→星期+时间、每月→日期(1-31)+时间（超当月最大天数自动取月末）、每隔几小时→间隔数字输入框。详见第 19.4 节。
- **订单状态简化**：移除 `virtual_shipped`（已虚拟发货）状态，全端统一为 **pending/virtual/shipped/refunded** 四态（后端 orders/statistics/dashboard、前端各组件、移动端均已清理）。
- **追溯码语义确认**：追溯码 = 订单号 order_id = 二维码内容；新建/编辑订单表单「订单号」标签统一为「追溯码」（22 位订单号输入框字号 14→12px 保证完整显示）；订单详情弹窗二维码下方显示追溯码（标签与数字分行、数字单行不换行）。
- **智慧大屏升级**：各模块固定高度 + 内容超出内部滚动（畅销品排行/网店排行/超期订单/统计卡/月度趋势）；超期订单卡片显示发货状态 + 生产进度上下结构标签、天数红色加粗、标题带「X单」计数；右侧面板超期订单与网店排行位置对调（超期在上）；超期订单接口返回 `produce_status`/`produce_status_text`。
- **生产统计口径**：已退货/退款（refunded）订单不计入「未生产/生产中/生产完成」统计——process-flow（老板工作台）、factory-dashboard（工厂看板）、overview（智慧大屏）、orders 列表 `produce_status` 筛选四口径统一排除，保证卡片数字 = 跳转列表数量。
- **菜单调整**：「费用统计（运费统计）」由「数据统计」模块迁入「财务模块」。
- **退款订单详情**：右侧抽屉新增三端图片分类展示（销售端/工厂端/发货端，按 images.layer 分组）；images 表 layer 全局规范化为 sales/factory/shipping（旧 base/detail/extra 已映射）。
- **订单列表增强**：二维码列前新增「序号」列；关键词搜索支持追溯码（=order_id）；组件监听 `route.query` 变化自动刷新（修复 keep-alive 缓存下工作台卡片跳转不刷新的问题）。
- **异常数据修复**：12 单「已发货但生产状态未完成」强制锁定为生产完成（operator=system-auto，写操作日志）；数据库已填充 100 条/表的业务测试数据（角色仅 boss/sales/factory/shipping，员工密码 123456）。
