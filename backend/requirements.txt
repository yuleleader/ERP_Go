# 版本放宽到支持 Python 3.13 的范围，让 pip 自动选择有现成 wheel 的最新兼容版本。
# 如需固定版本，可在安装成功后导出：python -m pip freeze > requirements.txt
fastapi>=0.115,<1
uvicorn[standard]>=0.34
pydantic>=2.10,<3
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
python-multipart>=0.0.9
sqlalchemy>=2.0,<3
aiosqlite>=0.19
qrcode>=7.4
aiofiles>=23.2
apscheduler>=3.10,<4
