# 全系统端到端测试报告

| 项目 | 信息 |
| --- | --- |
| 测试时间 | 2026-08-13 |
| 测试范围 | 全系统端到端走查（智慧大屏 / 工作台 / 订单管理 / 基础信息 / 数据统计 / 财务模块 / 系统设置 / 预警中心 / 数据导入）+ 4 角色权限显隐 + 深度交互 |
| 测试方法 | Playwright 真实浏览器走查 + 关键路径 API 直接调用 |
| 测试环境 | 临时隔离库（`backend/data/db/real_backup/order_system.db` 已备份）+ 启动器外手动起 8000/5173 |
| 测试数据 | 按系统业务规则填充：用户/类别/品牌/商品/物流/网店/订单(12 单覆盖各状态)/账务代码/收支/提现 |
| 工具 | Playwright + Chromium、Python urllib（API 调用） |

## 一、测试结果总览

| 维度 | 项目数 | 通过 | 失败 |
| --- | --- | --- | --- |
| 老板端全模块走查（页面 31 + 关键交互 4） | 35 | 35 | 0 |
| 四角色走查 + 权限显隐验证 | 14 | 14 | 0 |
| 深度交互（详情弹窗/Excel 导出/模板下载/预警/工资发放/退款列表） | 6 | 6 | 0 |
| 数据填充 + 接口冒烟（含订单/收支/提现/发货） | 28 | 28 | 0 |
| **合计断言** | **83** | **83** | **0** |

**结论：全系统功能无阻断性 Bug，发现 3 处真实产品 Bug，均已当场修复并复测通过。**

## 二、走查覆盖

### 2.1 老板端（1001）逐模块访问 31 页 + 4 关键交互
- 智慧大屏、工作台、订单管理、基础信息（商品/类别/品牌/网店/物流）、数据统计（数据总览/销售统计/毛利分析/网店销售统计/退款订单/退款率分析/预警中心/销售提成统计）、财务模块（工资结算/账户提现/运费统计/账务代码/收支录入/收支统计）、系统设置（用户管理/站内信/日志/备份/参数/清理/导入/系统信息）
- 关键交互：新建订单弹窗 / 编辑用户权限树 / 新建网店弹窗 / 新建商品弹窗（均能正常打开并渲染）

### 2.2 四角色权限显隐
| 角色 | 验证项 | 期望 | 结果 |
| --- | --- | --- | --- |
| 2001（sales，全权限） | 工作台流程全景/订单导出按钮/网店新建/编辑 | 全部可见 | ✅ PASS |
| 3001（sales，已授 export/add，未授 edit） | 订单导出/网店新建/网店编辑 | export✓ add✓ edit✗ | ✅ PASS（差异隐藏正确） |
| 4001（factory，权限极简） | 侧边栏无数据统计/财务模块/订单页无新建/无导出 | 全部隐藏 | ✅ PASS |
| 5001（shipping） | 工作台/订单页可访问（只读字段） | 可访问 | ✅ PASS |

### 2.3 深度交互
- 订单详情弹窗（含二维码、三端图片 tab）
- 销售统计 Excel 导出（生成 `销售统计-人员销售汇总-2026-08-12.xlsx`）
- 数据导入模板下载（生成 `订单导入模板-2026-08-12.xlsx`）
- 退款订单列表展示
- 工资结算发放按钮
- 预警中心加载

## 三、发现的产品 Bug 与修复

### Bug #1 — 类别编码到 999 后继续新增会 500（已修复）
- **现象**：快照库已存在一级类别 code "999"、"1000"，新增类别时接口返回 500 Internal Server Error
- **根因**：`backend/app/api/categories.py` 的 `_next_root_code` 用 `int(code[:CODE_WIDTH])` 取前三位计算 max；编码到 1000 后 `"1000"[:3]="100"`，max 计算错误（999 > 100），再次生成 "1000" 撞唯一约束
- **修复**：改为 `int(code)` 完整比较；二级编码同理按父编码长度截取
- **影响**：生产环境类别超过 100 个后会触发；用户体验为直接报错 500
- **复测**：连续新增多个一级/二级类别成功，编码连续递增

### Bug #2 — 创建订单若直接填「已发货」，生产状态未自动锁定（已修复）
- **现象**：通过 API 创建订单时 `shipping_status="shipped"` 但 `produce_status` 默认 `unproduce`，造成「已发货但生产未完成」数据不一致（项目此前修过此类问题，但遗漏了创建入口）
- **根因**：`backend/app/api/orders.py` 的 `create_order` 创建 `Order` 时未联动生产状态
- **修复**：新建 `Order` 时若 `shipping_status == "shipped"` 则同步设 `produce_status="produced"`
- **影响**：销售端直接录入已发货订单（如订单导入）会出现数据不一致；之前项目有 12 单类似问题强制锁定修复，本次补上根本原因
- **复测**：API 创建即发货订单 → 返回体 `produce_status="produced"`，数据库写入正确

### Bug #3 — 创建订单接口响应体缺生产状态字段（已修复，轻微）
- **现象**：`POST /api/orders/` 返回的 `OrderResponse` 中 `produce_status` 为 None（前端拿不到刚建订单的生产状态，需额外请求详情）
- **根因**：`create_order` 的 `order_dict` 手工映射漏了 `produce_status` / `produce_status_update_at` / `produce_status_update_user`
- **修复**：补上三个字段
- **复测**：创建接口响应包含 `produce_status=produced`

## 四、测试数据场景覆盖

- 用户：5 个（boss 1001 + sales 2001/3001 + factory 4001 + shipping 5001）按业务规则配置数据权限
- 类别：5 个（一级 3 + 二级 2）
- 品牌：3 个、商品：4 个、物流公司：2 个
- 网店：4 个（2001 创建 2 个、3001 创建 1 个、快照已有 1 个）
- 订单：12+ 单，覆盖 pending/unproduce、producing、produced、shipped（自动锁定）、virtual、refunded（全状态）
- 账务代码：5 个、收支：3 条、提现申请：2 条
- 通知：自然产生（工厂端接收新订单通知、发货端变更等）

## 五、注意事项 / 已知非问题

1. **订单管理仅配置「查询/导出」权限项**：销售端创建订单仍由角色制控制（销售可创建自己的订单、老板全权、工厂/发货不可），不在数据权限拦截范围——这是按设计规格保持
2. **发货端改生产状态被拒**：发货端只能改发货字段，不可改生产状态；发货成功后系统自动锁定生产完成——这是正确拦截，非 bug
3. **GET 接口不单独拦截**：避免影响录订单时选网店等跨模块下拉依赖；菜单隐藏已足够前端拦截，恶意直接调接口可见但 POST/PUT/DELETE 已全覆盖

## 六、测试产物（不入 git）

- 测试脚本：`_walk_boss.py` / `_walk_roles.py` / `_walk_deep.py` / `_seed_data.py` / `_seed_data2.py` / `_perm_ui_test.py` / `_perm_ui_test2.py`
- 截图：`_shots/test/`（约 45 张）
- 临时数据库：`backend/data/db/real_backup/order_system.db`（真实库原文件备份，测完已恢复原位）

测试完成后已停掉测试用前后端进程并把真实数据库从 `backend/data/db/real_backup/` 恢复回 `order_system.db`，业务数据库零污染。

## 七、提交记录

修复 3 个 Bug 的代码改动：
- `backend/app/api/categories.py`：类别编码生成逻辑修复（Bug #1）
- `backend/app/api/orders.py`：创建订单生产状态联动 + 响应体字段补齐（Bug #2 + #3）